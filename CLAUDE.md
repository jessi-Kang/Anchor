# Anchor — 프로젝트 지침

Jessi(PM, 한국어 화자)의 개인 언어학습 웹앱. 영어·일본어·스페인어.
"가르치는 서비스"가 아니라 **"내가 이미 아는 것에서 한 걸음 떨어진 것을 발견하고 소리로 꺼내게 만드는 작업실"**.
**인풋 없이도 돌아가고, 인풋이 있으면 더 나에게 맞아진다.**

> **`README.md` 의 「근간」 절이 이 파일보다 위다** (2026-09-21 Jessi 확정). 이 파일 어느 줄이든 그 절과 어긋나면 그 절이 맞다.

전체 기획은 `docs/SPEC.md`, 화면 순서·각 화면의 목적·나가는 길은 `docs/FLOW.md` 가 기준. 이 파일은 코딩할 때 항상 지켜야 하는 것만 추린 것.

## 절대 원칙 (기능 하나를 만들 때마다 확인)

0. 목표는 대화. 모든 학습 항목은 말할 상황에서 출발해 말하기로 끝난다.
1. 설명하지 말고 발견시켜라. 규칙·뜻을 먼저 보여주지 않는다. 사용자가 먼저 추측 → 확인. 문법 설명 텍스트 금지.
2. 아는 것 위에만 쌓아라. 시작점은 항상 이미 아는 것. **일본어는 한국어 한자어의 뜻과 소리가 앵커이고, 새로 배우는 것은 글자와 일본어 읽기다** — 협력은 뜻도 소리도 아는데 `協` 은 못 읽는다. **그 간격이 이 제품이 사는 자리다.** 스페인어는 영어 어근, 영어는 업무 어휘. 정의는 그 언어 안에서 쉬운 단어로(롱맨 방식, 영어 기본 2,000어). 한국어는 앵커 호출용으로만 쓰고 설명 언어로 쓰지 않는다.
3. 소리는 덩어리·루프·눈으로. 연음·억양 규칙 텍스트 금지. 덩어리("push this to") 단위로 듣기 → 따라 말하기 → 곡선 겹쳐 보기.
4. 커리큘럼 없음. 학습 순서는 앵커 그래프("아는 것 + 1")가 정한다. 복습은 카드 반복이 아니라 **다음에 만나는 것에서의 재만남** — 내가 넣은 자료일 수도, 우리가 가진 예문일 수도 있다. **자료가 0개인 날에도 재만남이 돈다.**

## 하지 않는 것
- 레벨 테스트, "초급/중급/고급" 질문, 자기 보고식 수준 입력. "아는 것"은 **실제로 만난 항목**에 대한 행동(알아/몰라, 추측)으로만 채운다 — 내 자료에서 온 것이든 그래프가 고른 것이든
- 세 언어를 한 세트로 강제하는 것. 언어는 하나씩 켠다. 언어를 켜면 **상황 고르기 몇 탭으로 앵커를 심고 바로 첫 카드로 간다.** 자료 넣기(F02)는 **선택 가지**이지 거쳐야 하는 문이 아니다.
- **미리 채우는 덱(씨앗 카드 40장 넘기기).** 막는 것은 **첫 발견 앞의 그라인드**이지 몇 탭이 아니다 — **상황 고르기는 금지가 아니라 필수다**(그걸로 앵커가 그래프에 심긴다, `README.md` 근간). 다만 울타리 셋: **몇 탭 안에 끝나고 · 끝나면 바로 첫 카드로 가고 · 레벨을 묻지 않는다.** 가나 확인은 일본어 첫 카드 직전에 한 번만. **첫 5분 안에 "내가 맞힌 한자"가 하나 생겨야 한다** (`docs/FLOW.md` 0장).
- 뒤로 갈 길이 없는 화면. 상단 왼쪽 "지금 어디" 라벨은 한 단계 위로 가는 링크. 어느 화면에서도 홈까지 2탭.
- 내부 용어(씨앗·노드·앵커·그래프에 심는 중)를 화면에 쓰는 것.
- 스트리크·포인트·리더보드·공개 프로필·친구 기능
- 텍스트 채팅형 회화, 문법 교정
- 플래시카드 반복(SRS)
- 한자 쓰기·필순

## 데이터 원칙 (전제, 협상 불가)
- Google 로그인 (Neon Auth). 모든 테이블에 `user_id`, Postgres RLS로 계정 간 분리. 사용자 1명이어도 동일.
- 기본 비공개. 공개는 항목 단위로 사용자가 명시적으로 켤 때만.
- 데이터 유실 = 즉시 이탈. 백업은 세 층이다: Neon PITR(몇 분 전으로) · 배포 전 스냅샷(마이그레이션 사고) · Neon 밖 매일 JSON 백업(최악의 경우). 분기마다 복구 리허설. 보존 기간과 절차는 `docs/BACKUP.md` 한 곳에 있다.
- 전체 내보내기(JSON) 언제든. 계정 삭제 시 백업 사본까지 삭제.
- 음성 녹음은 계정 전용 암호화 스토리지, 원본은 일정 기간 뒤 삭제(설정 가능), 피치 데이터만 보관. 기간은 `docs/SPEC.md` 6.6 데이터 원칙.
- 오프라인/저장 실패 시 브라우저 임시 보관 후 재전송. 추측 한 번, 녹음 한 번도 유실 없음.
- 사용자 자료를 외부 LLM에 보낼 때 학습에 쓰지 않는 설정으로 호출.

## 스택
- Next.js (App Router), TypeScript, 모바일 우선 PWA. 네이티브 앱 아님.
- Vercel 배포. Neon Postgres + Neon Auth(Google OAuth).
- Claude API: 한자·어근 분해, 후킹 질문 생성, 쉬운 정의 생성(정의 어휘 제한), 덩어리 추출.
- ElevenLabs TTS (+ Jessi 목소리 클론 voice_id `pwjMkbtUbj1hBa0RkN5N`: 목표 발음을 "내 목소리 버전"으로 들려줘 음색 차이 제거).
- Web Audio API + 피치 검출(브라우저에서 처리). 
- 데이터: KANJIDIC2, 한자 구성요소(IDS), 한국 한자음 사전, 영·서 어원 사전(공개).

## 앵커 그래프 데이터 모델 (핵심)
- 노드: 부품(한자 부수, 어근, 문법 구조, 대화 덩어리) 또는 단어. 언어 무관 공통 테이블.
- 엣지: "부품 ∈ 단어", "한국 한자음 ↔ 음독", "영어 어근 = 스페인어 단어", "기능 분류(태도 9종)".
- 사용자별 노드 상태는 3층 분리: `knows_sound`, `knows_meaning`, `can_say`. "협력은 아는데 協은 모른다"가 표현돼야 함.
- **다음 카드 = 아는 노드에서 가장 가까운 모르는 노드.** 고르는 함수는 `inputs` 가 아니라 **`user_node_state`(아는 노드)에서 출발한다** — 인풋은 후보 집합에 **가중치를 더할 뿐**이고, 인풋이 0개여도 함수가 돈다. 추측 결과로 인접 노드 확신도 보정.
- **자료를 안 넣은 날에도 큐는 찬다. 빈 상태 화면에 "자료를 넣어야 시작할 수 있어요"가 뜨면 버그다.** 그 큐를 **어디서 고르는지**(자료 한 편이냐 그래프 전체냐)는 **지금 열린 물음이라 보류다**(`docs/SPEC.md` 9장). 한때 「내장 코퍼스」라는 장치 이름으로 적어 두었는데, 그건 물음을 장치에 맞춘 것이었다. **확정 전에 이 장치로 화면·문구를 설계하면 버린다** — 다만 위의 「뜨면 버그다」는 어느 답에서도 그대로다.

## 디자인 시스템 (design/tokens.json, design/screens/*.html 참고)
- 텍스트 세기 3단계로만 위계: T1 #2A2D33(제목·본문), T2 #6B7078(보조), T3 #9AA0A8(라벨). 순수 검정 금지.
- 강조는 글자색이 아니라 배경 틴트 #E9EEF6 (+ 글자 #3F5E90). 채도 높은 색 금지. 그린·앰버 계열 금지.
- 배경 #F6F6F7, 카드 #FFFFFF + 1px #E6E7EA, 반경 18px. 버튼은 #2A2D33 검정 하나(56px, 반경 14px), 화면당 주 버튼 1개.
- 폰트: Noto Sans KR / Noto Sans JP만. 세리프·모노스페이스 금지. 최소 13px, 본문 15px+, 한자 표시 44~132px.
- 누르는 것의 탭 영역은 44px 이상. 보이는 크기가 아니라 영역 기준이라, 글자만 있는 링크는 여백으로 영역을 키우되 레이아웃을 밀지 않는다.
- 화면마다 `h1` 은 하나. 그 아래는 건너뛰지 않고 차례로 내려간다(h1 → h2 → h3). 상단 "지금 어디" 라벨은 제목이 아니다.
- 뜻을 나르는 그림·아이콘에는 대체 텍스트를 단다. 장식이면 빈 값으로 두어 읽히지 않게 한다. 한자 글자는 그림이 아니라 텍스트로 넣는다.
- 화면 뼈대 공통: 상단 작은 회색 "지금 어디"(홈 / 카드 1/4 / 아침 기사) + 흰 카드 안 작은 라벨 + 하단 검은 버튼 1개.
- 한 화면에 한 가지. 발견 카드는 5장면(출처 → 아는 것 → 부품 → 추측 입력 → 정답 → 착지)을 넘기는 구조, 절대 한 화면에 합치지 말 것.
- 그래프·곡선에는 항상 범례.
- 일본어 텍스트에는 항상 よみがな(ruby). 한자에만 달고 가나·가타카나에는 달지 않는다. 읽기를 화면 본문으로 크게 보여 주는 자리는 중복해서 달지 않는다. **판정이 일어나는 자리에서는 읽기를 가린다** — 카드는 그 카드에서 맞혀야 할 한자를 추측 입력 전까지, 재만남(F12)은 이미 만난 한자를 탭해서 열기 전까지 (`docs/FLOW.md` 4장).
- ruby 는 **한자가 학습 대상으로 나오는 자리(자료 문장·부품·카드)에만** 단다. UI 문구 안에서 항목을 가리키는 한자(홈의 다음 카드 이유 한 줄, 자료 제목)에는 달지 않는다 — 그 자리의 한자는 읽으라고 있는 게 아니라 어떤 항목인지 가리키는 이름표다. 규칙대로 달면 아직 안 푼 한자가 홈에 빈칸으로 떠서 원칙 1 을 거스른다. 발견은 카드에서 일어난다.

## 사이트맵·로고
- 라우트 구조는 `docs/SITEMAP.md`. 언어는 라우트가 아니라 데이터 속성. 하단 탭바 없음.
- 로고는 A안으로 확정. `design/logo/anchor-mark.svg`(마크), `anchor-lockup.svg`(가로 조합), `anchor-appicon.svg`(앱 아이콘), `anchor-mark-mono.svg`(단색)를 그대로 쓴다. `design/logo/alternatives/`는 탈락안이라 쓰지 않는다. 새로 그리지 않는다. PWA 아이콘은 anchor-appicon.svg에서 512/192 maskable로.

## MVP 순서 (docs/SPEC.md 9장)
1. 인증 + 데이터 원칙(RLS, 백업, 내보내기) 먼저. 화면보다 먼저.
2. 첫 방문: 로그인(O01) → 언어 고르기(O02a) → **상황 고르기(앵커 심기)** → 첫 카드. **자료 넣기(F02)는 선택 가지로 옆에 붙고, 지나가야 하는 문이 아니다.** 뽑기(F03, 알아/몰라)는 자료를 넣은 사람이 가는 자리다.
3. 일본어 한자 발견 카드 (가나 1회 O03 → F04 + Scene1~5 → F10 듣기·말하기) + 그래프 1장 (F11) + 홈 (F01).
4. 영어 대화 루프 (F13 → F17 → F14): 못 한 말 → 내가 먼저 추측 → 덩어리 → 듣기/말하기/곡선.
5. 재만남 (F12) + 하루 끝 (F15).
6. 두 번째 만남: 어제 것으로 돌아가는 길(재만남·지난 덩어리·지난 자료), 저장 실패 재전송, 내보내기 확인.
**지금 만드는 순서는 6단계를 3단계 뒤로 당긴다.** 통과 기준이 「2주 후 재만남 인식률 70%」인데 재만남이 마지막에 오면 **그걸 잴 분모가 마지막까지 안 생긴다.** 당긴 순서: ① 첫 방문이 Jessi 가 보는 창에서 끝까지 간다 → ② 둘째 날에 열 것이 홈에 보인다(재만남) → ③ 그다음이 값·시험·나머지. **① 이 ② 앞인 것이 중요하다 — 첫날 5분을 못 지나면 둘째 날이 없다.**
검증 기준: 2주 후 재만남 인식률 70%, 같은 덩어리 5회차 원어민 대비 곡선 일치도 상승. 그 둘을 **잴 수 있는 상태**가 6단계의 기준이다 (`docs/SPEC.md` 9장).

## 작업 방식
- 기능 하나 = 화면 하나. `design/screens/`의 해당 HTML을 열어 레이아웃·문구를 그대로 옮긴다. 문구를 임의로 늘리지 않는다.
- **참고 HTML 은 위계의 근거지 개수의 근거가 아니다.** 참고는 390×844 틀에 그려져 있고 Jessi 가 보는 창은 360×650 이다(`docs/QA.md` 2장). 「참고대로 옮긴다」와 「주 버튼이 스크롤 없이 닿는다」가 부딪치면 **접히지 않는 쪽이 이긴다** — 몇 개가 한 화면에 드는지는 그린 캔버스 크기의 부산물이라 근거가 못 된다. 배치 의도는 그대로 옮기고 **개수·여백은 실제 창에 맞춘다**(`docs/FLOW.md` 4장).
- 새 화면을 만들면 위 원칙 0~4와 "하지 않는 것"에 걸리는지 스스로 점검하고, 걸리면 만들지 않는다.
- Jessi에게 "초급/중급/고급"을 묻는 UI가 생기면 버그다.

## 브랜치
- `main`이 프로덕션이다. GitHub 기본 브랜치이고 Vercel이 여기서 배포한다.
- 세션 작업 브랜치에 커밋한 뒤, 검증(typecheck·lint·build)이 통과하면 같은 커밋을 `main`에도 바로 푸시한다(fast-forward). Jessi가 따로 합치지 않는다. PR·리뷰 단계 없음.
- 작업 브랜치는 세션이 끝나면 지워도 된다. `main`에 없는 커밋을 남기지 않는다.

## 커밋 규칙

이 레포의 커밋 메시지는 vibelog가 매일 밤 읽어 데브로그 글과 쇼츠 영상을
자동 생성하는 원료다. 커밋 메시지가 얇으면 글도 얇아진다.

- 한국어로 쓴다. 형식: `타입: 한 줄 요약` + 빈 줄 + 본문.
- 타입은 feat / fix / docs / chore / design / asset 중 하나. fix는 실제로
  깨졌던 것을 고쳤을 때만 쓴다 — 영상의 커밋 그래프가 타입별로 색을 입히고,
  fix 커밋은 글의 "삽질 포인트" 재료가 된다.
- 본문에 **"왜"**를 반드시 한 문장 이상 쓴다. 뭘 바꿨는지는 diff가 말해주니,
  왜 그렇게 했는지·뭘 시도하다 왜 버렸는지를 남긴다.
- 버그를 고친 커밋은 증상 → 원인 → 해결을 본문에 남긴다.
- 작업 단계가 끝날 때마다 커밋한다. 하루치를 한 커밋에 뭉치지 않는다.
- 커밋 메시지는 공개 블로그에 그대로 노출된다. 비밀 키·내부 URL·개인정보를
  쓰지 않는다.
