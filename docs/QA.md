# 검증 플로우 (QA 기준 문서)

무엇을 어떤 순서로 눌러 보고, 각 지점에서 무엇이 참이어야 하는지를 이 문서가 정한다. 소유는 `Anchor · QA`(`docs/TEAM.md` 1장).

판정 기준은 `docs/FLOW.md` 다. 코드가 FLOW 와 다르면 코드가 틀린 것이고, FLOW 자체가 어색하면 기획 문제다. **QA 는 사실을 대고 등급을 매긴다.** 코드 문제인지 기준 문서 문제인지, 완결로 볼 수 있는지는 PM 과 기획이 판단한다(`docs/TEAM.md` 3장).

## 1. 범위

| 한다 | 하지 않는다 |
| --- | --- |
| 화면을 실제로 눌러 보고 결함을 찾는다 | 코드를 고친다 |
| 항목마다 통과·실패와 등급을 적는다 | 완결을 선언한다 |
| 실패를 FLOW 의 어느 절과 어긋나는지로 댄다 | 무엇을 고칠지 설계한다 |
| 코드 결함은 개발에, 기준 문서 문제는 PM 에 보낸다 | 같은 건을 두 곳에 보낸다 |

## 2. 준비

### 계정

한 라운드는 **초기화된 계정**으로 돈다. 이전 라운드의 언어·자료·카드 상태가 남아 있으면 첫 방문 경로가 그대로 재현되지 않는다.

초기화는 `POST /api/account/delete`(body `{"confirm":"삭제"}`)로 한다. 로그인한 상태에서만 되고, 복구되지 않는다. 프로덕션 계정에 쓰지 않는다.

### 띄우는 법 — 실계정 (판정에 쓰는 것)

로컬 Postgres 와 시험 로그인으로 돈다. 개발이 자기 컨테이너에서 확인한 순서 그대로다.

```bash
# 1. Postgres. 루트로는 initdb 가 안 되고, 경로는 /var/lib/postgresql/ 아래여야 한다 (스크래치패드는 postgres 계정이 못 지나간다)
mkdir -p /var/lib/postgresql/anchor && chown postgres:postgres /var/lib/postgresql/anchor
su postgres -c "/usr/lib/postgresql/16/bin/initdb -D /var/lib/postgresql/anchor/data -A trust -U postgres"
su postgres -c "/usr/lib/postgresql/16/bin/pg_ctl -D /var/lib/postgresql/anchor/data \
  -o \"-k /var/lib/postgresql/anchor -p 5433 -c listen_addresses=127.0.0.1\" \
  -l /var/lib/postgresql/anchor/log start -w"
# 2. DB 이름은 반드시 neondb (0001 이 GRANT CONNECT ON DATABASE neondb 를 박아 둬서 다른 이름이면 멈춘다)
createdb -h 127.0.0.1 -p 5433 -U postgres neondb
# 3. 환경 변수 파일 (값은 전부 로컬 더미. ANCHOR_APP_PASSWORD 와 ANCHOR_DATABASE_URL 의 비밀번호는 같아야 하고, 쿠키 비밀값은 32자 이상)
#    DATABASE_URL_ADMIN=postgresql://postgres@127.0.0.1:5433/neondb
#    ANCHOR_APP_PASSWORD=<더미>   ANCHOR_DATABASE_URL=postgresql://anchor_app:<더미>@127.0.0.1:5433/neondb
#    NEON_AUTH_BASE_URL=https://local.invalid/neondb/auth   NEON_AUTH_COOKIE_SECRET=<32자 이상>
#    ANCHOR_TEST_LOGIN=1   ANCHOR_TEST_USER_ID=qa-local-1
. /var/lib/postgresql/anchor/env.sh
# 4. 스키마와 씨앗. db:seed 를 건너뛰면 F03 에서 한자가 하나도 안 뽑힌다 — 그건 결함이 아니다
pnpm db:migrate && pnpm db:seed
# 5. 앱
pnpm dev
```

시험 로그인: `GET /api/test-login` 이 `anchor_test_session` 쿠키(12시간)를 심고 `/today` 로 보낸다. 새 계정이면 `/today` 가 `/onboarding/languages` 로 보내는 것이 정상이다. 계정 행은 `/today` 가 만든다. 나가기는 `/api/test-login?logout=1`. 이 라우트는 `route.dev.ts` 라서 `ANCHOR_TEST_LOGIN=1` 인 로컬 dev 에만 존재한다.

라운드마다 초기화는 `POST /api/account/delete` (`{"confirm":"삭제"}`). **로컬에서는 502 가 정상이다** — Neon Auth 주소가 자리표시자라 마지막 단계만 실패하고 DB 는 이미 비워진 뒤다. 프로덕션에서는 200 이다.

라운드를 돌기 전에 `git merge origin/main` 뒤 `pnpm install` 을 한다. 의존성이 낡으면 typecheck·build 가 깨지는데 이건 코드 결함이 아니라 내 환경 문제다. 화면이 지워졌는데 `.next/dev/types` 가 남아 있어도 같은 증상이 난다.

### 띄우는 법 — 미리보기 (모양만)

`ANCHOR_DESIGN_PREVIEW=1` 은 로그인·DB 없이 예시 데이터로 화면을 그린다. 모양·문구·접근성은 이 모드로 봐도 된다. **상태가 바뀌는 항목은 이 모드로 판정하지 않는다.** `src/lib/env.ts` 가 모듈 평가 시점에 환경 변수를 요구하므로 형식만 맞는 더미 값이 있어야 한다.

### 뷰포트

390×844. 모바일 우선이 전제이므로 이 크기에서 나온 것만 결함으로 센다.

### 결함이 아닌 것

- `/talk` 은 영어를 켜지 않으면 307 → `/today`. F13·F14 는 설정에서 영어를 켠 뒤 본다.
- `/graph` 는 `?card=` 없이 열면 307 → `/today`. F11 은 카드에서 들어가는 화면이다.
- `ANTHROPIC_API_KEY` 가 없으면 카드 문안은 사전 조합(`content_source: fallback`)으로, F14 영어 문장은 쓴 한국어 줄 그대로 나온다. `ELEVENLABS_API_KEY` 가 없으면 `/api/tts` 는 204 이고 F10·F14 는 "브라우저 음성이야" 한 줄을 보인다. 키 없는 환경의 정상 동작이다.
- 헤드리스 브라우저에는 마이크가 없다. Chromium 을 `--use-fake-ui-for-media-stream --use-fake-device-for-media-stream` 으로 띄우고 `microphone` 권한을 주면 가짜 마이크가 생겨 O03 가 "듣는 중"이 되고(인식은 0개) F10·F14 "말하기"가 실제로 3초 녹음해 곡선을 그린다. 그 없이 띄우면 O03 는 곧바로 마이크 착지(O03b)로 가고 "말하기"는 "마이크를 허용하지 않았어"로 끝난다 — 둘 다 정상.

## 3. 첫 방문 경로

`docs/FLOW.md` 1장 순서를 그대로 따른다. 일본어를 고른 경우다. **전체가 5분 안, 버튼 17번 안에 끝나야 한다**(FLOW 1장).

| # | 화면 · 라우트 | 누르는 것 | 그 순간 참이어야 하는 것 |
| --- | --- | --- | --- |
| 1 | O01 `/` | Google로 계속하기 | 데이터 원칙 2줄이 보인다. 로그인 뒤 켠 언어가 0개면 2로 간다 |
| 2 | O02a `/onboarding/languages` | 일본어 행 탭 → 다음 | 아무것도 안 고르면 "다음"이 눌리지 않는다. 행 부제가 출발점을 말한다("한국어 한자음에서 시작"). 첫 방문에는 이탈 링크가 없다. 행을 탭하면 알약이 켜짐/꺼짐으로 바뀐다(FLOW 1장 2·1′장. 언어는 켜고 끄는 것이라 알약이다) |
| 3 | F02 `/inputs/new?lang=ja` | 예시 자료 탭 → 모르는 것만 뽑기 | 붙여넣지 않아도 예시 자료 1개를 탭 한 번으로 넣을 수 있다. 예시 문구는 `design/screens/F02.html` 그대로다 |
| 4 | F03 `/inputs/[id]` | 한자 행의 "알아 / 몰라" → 協부터 | 한자가 두 묶음("아는 소리에서 시작" · "발판 없음")으로 갈린다. 행에 "먼저/나중에"가 따로 없다. **"알아"를 누른 한자는 카드 큐에서 빠져 "아는 것" 묶음으로 내려간다.** 몰라·미표시는 큐에 남는다. "알아"는 뜻을 아는 것(`knows_meaning`)으로 저장되고 소리(`knows_sound`)는 따로 묻지 않는다(`docs/SPEC.md` 6.5). 40장 덱이 없다 |
| 4′ | O03 `/onboarding/kana` | 읽기 시작 → 다 읽었어 | **"읽기 시작"을 누르기 전에는 마이크가 켜지지 않는다.** 권한 팝업도 그전에 뜨지 않는다. 켜져 있으면 표시되고 끌 수 있다. **인식이 0개면 "다 읽었어"가 통과로 기록되지 않는다.** 일본어 첫 카드 직전에 한 번만 나온다. 상단 라벨은 F03(그 자료)로 간다(한 단계 위. PM 판정, FLOW 에 채워질 예정) |
| 5 | F04 `/cards/[id]` | 협부터 풀어보기 | 자료 문장 안에서 그 한자가 든 단어가 강조된다. **이 카드에서 맞혀야 할 한자의 よみがな는 여기서부터 빈칸이다.** 다른 한자에는 달려 있다. 상단 라벨을 누르면 4로 간다 |
| 6 | Scene1~5 `/cards/[id]/1..5` | 장면마다 버튼 1개 | 다섯 장면이 각각 한 화면이다. **추측 입력이 정답보다 먼저 온다.** 진행 막대는 6칸이고 카드 안 위치만 센다. **대상 한자의 읽기가 Scene3 전까지 빈칸이고, 빈칸 폭이 읽기 글자 수를 따르지 않는다.** 읽기가 두 글자인 한자와 세 글자인 한자의 빈칸 폭이 같아야 한다. 폭이 글자 수를 따라 늘어나면 정답 길이가 새는 것이라 결함이다. Scene4·Scene5 에서는 읽기가 보인다 |
| 7 | F10 `/cards/[id]/speak` | 듣기 → 말하기 → 됐어, 다음 | 읽기를 소리로 들려준다. 읽기가 글자로도 보인다(정답 뒤라 가리지 않는다). 상단 라벨을 누르면 5로 간다 |
| 8 | F11 `/graph?card=` | 다음 카드 → 5 | 다음 카드를 고른 이유가 한 줄 있다. 그래프에 범례가 있다. **"아는 것" 동그라미에는 판정된 노드만 있다** — 알아로 표시했거나 카드에서 맞힌 것. 카드에서 보여 준 부품(十·力)은 판정된 것이 아니라 여기 못 온다. 같은 한자가 "아는 것"과 "다음"에 동시에 있지 않다(FLOW 1장 8·4장) |
| 9 | F01 `/today` | — | 방금 넣은 자료가 1행으로 있고 부제가 개수만 말한다("한자 4개 · 남은 카드 3"). 카드 라벨이 "오늘"이다 |

### 갈래

| 갈래 | 누르는 것 | 참이어야 하는 것 |
| --- | --- | --- |
| 가나를 못 읽음 | 4′ 에서 "못 읽겠어" | O03b 1장을 보인 뒤 **그대로 5로 간다. 카드가 잠기지 않는다**(FLOW 4장). O03b 문구는 "가나부터 익히는 길은 준비 중이야. 지금은 읽기를 소리로 들려줄게." 그대로이고 다른 요소가 없다. 주 버튼은 "첫 카드로", 상단 라벨은 홈. 결과는 `kana_module` 로 남는다 |
| 마이크 미지원·거부 | 4′ 진입 | **통과로 기록되지 않는다.** O03b 레이아웃에 "마이크를 쓸 수 없어 / 다음에 다시 확인할게."를 1장 보인 뒤 5로 간다 |
| 마이크는 켜졌는데 인식 0개 | 4′ 에서 읽기 시작 → 아무 말 없이 다 읽었어 | **통과가 아니다.** 미지원·거부와 같은 착지(위 줄)로 간다. 통과로 남으면 안 한 일이 통과로 기록되는 것이라 헷갈림이다 |
| 언어를 둘 이상 고름 | 2에서 두 개 탭 | 첫 언어의 F02 로 가고, 나머지는 홈에서 이어서 한다. **순서는 고른 순서다**(코드 고정 순서가 아니다) |

### 경로 전체

| 무엇 | 기준 |
| --- | --- |
| 끊김 | 1에서 9까지 한 번도 막히지 않는다 |
| 404 | 0건 |
| 시간 | 5분 안 |
| 버튼 | 17번 안 |
| 이유 없는 화면 | 0장. "왜 하는지 모르는 화면"이 없다 |

## 4. 화면 공통

경로 위의 모든 화면에서 매번 본다. `docs/FLOW.md` 4장과 `CLAUDE.md` 디자인 시스템이 근거다.

| 무엇 | 참이어야 하는 것 |
| --- | --- |
| 뒤로 | 상단 왼쪽 "지금 어디" 라벨을 누르면 한 단계 위로 간다. 어느 화면에서도 홈까지 2탭 이내 |
| 이탈 | 한 화면에 이탈 링크 1개. 의미가 반대인 두 이탈을 같은 화면에 두지 않는다. 확인 화면의 실행 링크("지울게")는 이탈이 아니라 그 화면이 하는 일이라 따로 센다 |
| 주 버튼 | 검은 주 버튼 1개. 고를 것이 둘이면(알아/몰라, 언어) 둘 다 검은 버튼이 아니라 선택 칩이다. **되돌릴 수 없는 일은 검은 주 버튼에 없다.** 주 버튼은 돌아가는 쪽이고, 실행은 그 아래 회색 링크이며 확인 입력 전에는 꺼져 있다 |
| 어휘 | 내부 용어(씨앗·노드·앵커·그래프에 심는 중)가 화면에 없다. 상태 어휘는 아는 것 / 오늘 / 다음 세 개 |
| 조사 | 한자 뒤 조사는 글자가 아니라 한국 한자음 소리로 고른다. 十은 "십"이라 "十을", 支는 "지"라 "支가". 홈·F11·F04 어디서든 같다. 화면에 안 보이는 문자열(그래프 `aria-label`)도 같다 — DOM 을 본다(FLOW 4장) |
| 자료 이름 | 같은 자료는 F02 예시 행·홈·F03·F04·F11 에서 같은 이름이다. 예시 자료는 언어마다 — 일본어 "아침 기사", 영어 "회의 노트", 스페인어 "안내문". 사용자 자료는 제목 그대로, 제목이 없거나 12자를 넘으면 "내 자료". 이름 뒤 로/으로는 받침 없음·ㄹ 이면 "로"(아침 기사로·회의실로), 그 밖엔 "으로"(안건으로). 잘린 제목(…)이 화면에 없다. 상단 라벨은 이름만, 문장 안에서는 "아침 기사에서"(FLOW 4장) |
| 알약 | 알약은 시스템 상태이고 켜짐 / 꺼짐 / 이어서 세 개뿐. 알아/몰라는 선택 칩이다. 언어 켜기(O02a·설정)는 켜고 끄는 것이라 켜짐/꺼짐 알약이다(1장 2·1′장) |
| 단계 표시 | "일본어 2 / 3" 같은 온보딩 단계 표시가 없다. 세는 것은 카드 안의 "카드 1 / 4"와 진행 막대 6칸 둘뿐이고 섞이지 않는다 |
| 시간 | 홈 자료 행에 소요 시간이 없다. 시간은 F15 마지막 줄에만 쓴다 |
| 글자색 | T1 #2A2D33 · T2 #6B7078 · T3 #9AA0A8 밖의 글자색이 없다. 순수 검정이 없다 |
| 강조 | 강조가 글자색이 아니라 배경 틴트다. 채도 높은 색, 그린·앰버가 없다 |
| 글자 크기 | 13px 미만이 없다. 본문이 15px 이상이다. 한자 표시가 44~132px 안이다 |
| 폰트 | Noto Sans KR / Noto Sans JP 뿐이다. 세리프·모노스페이스가 없다 |
| 제목 | 화면마다 `h1` 이 하나다. 그 아래는 건너뛰지 않고 차례로 내려간다(h1 → h2 → h3). 상단 "지금 어디" 라벨은 제목 태그가 아니다 |
| 대체 텍스트 | 뜻을 나르는 그림·아이콘에 대체 텍스트가 있다. 장식은 빈 값이라 읽히지 않는다. 한자는 그림이 아니라 텍스트다 |
| 탭 영역 | 누르는 것의 세로·가로가 44px 이상이다. 보이는 크기가 아니라 영역 기준이다. `pnpm design:lint` 가 참고 HTML 쪽을 검사한다 |
| 그래프 | 그래프·곡선에 범례가 있다 |
| 마이크 | 사용자가 버튼을 누른 뒤에만 켜진다 |
| ruby | 일본어 한자에 よみがな가 있다. 가나·가타카나에는 없다. 읽기를 본문으로 크게 보여 주는 자리에 중복해서 달지 않는다 |

글자색·강조·글자 크기·폰트·제목·대체 텍스트·탭 영역·그래프·ruby 는 `CLAUDE.md` 디자인 시스템, 나머지와 ruby 를 가리는 시점은 `docs/FLOW.md` 4장이 근거다. 주 버튼 줄은 둘 다에 있다. 기준 문서 밖의 줄은 없다.

## 5. 첫 방문 밖

첫 방문 경로가 통과한 뒤에 돈다(`docs/FLOW.md` 1′장).

| 화면 · 라우트 | 누르는 것 | 참이어야 하는 것 |
| --- | --- | --- |
| F01 홈 빈 상태 `/today` | — | 켠 언어가 하나면 자료 행 없이 "첫 자료 넣기" 버튼 하나. **켠 언어가 둘 이상이면 언어마다 행 하나**(고른 순서, 부제는 출발점 "영어 어근에서 시작") + 주 버튼 "자료 넣기"(첫 언어). 행을 탭하면 그 언어의 F02. 둘째 언어로 가는 길이 여기 말고는 없다(FLOW 1′장) |
| 영어 뽑기 안내 `/inputs/[id]` (영어 자료) | 홈으로 | 영어 자료를 뽑으면 F03 대신 안내 1장 "자료는 저장됐어 / 영어 뽑기는 다음 단계야." 주 버튼 "홈으로", 이탈 없음(FLOW 1′장) |
| F01 홈 `/today` (다 본 자료) | 다 본 자료 행 → F12 | **카드를 다 끝내도 홈은 홈이다** — 어디로도 보내지 않는다. 자료 행은 세 상태: 안 뽑음 → F03, 남은 카드 → F04(이어서), 다 봄 → 재만남 F12(부제 "한자 21개 · 다 봤어"). 이유 한 줄 자리에 "이 자료는 다 봤어. 내일 또 나오면 그때 만나." 자료 넣기·못 한 말·설정이 그대로 선다(12차) |
| F01 홈 `/today` | 카드가 남은 자료 행의 "이어서" 알약 | 알약 어휘 "이어서"가 실제로 쓰이는 유일한 자리다. 탭하면 F04(그 자료의 다음 카드)로 간다. 카드가 남지 않은 행에는 알약이 없다 |
| F12 재만남 `/inputs/[id]/read` | 상단 라벨 · 읽기 끝 | 틴트의 기준은 "이미 만났는가" 하나 — F03 알아, 카드 끝냄(맞혔든 틀렸든). 몰라·미표시는 진하다. **틴트는 글자마다, 읽기는 덩어리마다**: 덩어리에 안 만난 글자가 하나라도 있으면 읽기는 통째로 빈칸(妥協 → 協만 틴트, 읽기는 빈칸). 만난 덩어리의 읽기는 보이고, 읽기가 없으면 빈칸을 안 단다. **안 만난 덩어리의 읽기는 HTML·RSC 페이로드에도 없다**(`inputs.meta.readings` 에 덩어리별 서로 다른 읽기를 넣고 문자열로 찾는다). 개수 한 줄은 진한 것만 세고, 하나면 이름("새로 배울 건 妥 하나뿐."), 여럿이면 개수. 상단 라벨 → F03(그 자료), "읽기 끝" → 홈. 일본어가 아닌 자료는 그 자료의 뽑기로, 없는 id 는 404 (FLOW 1′장) |
| F15 오늘 끝 `/today/done` | 내일 아침에 | **마지막 카드를 착지한 직후에만** 온다 — F11 의 마지막 버튼이 계정 전체 남은 카드 0이면 "오늘 켜진 것" → F15, 하나라도 남으면 "홈으로". 홈이 여기로 보내지 않는다(12차, c240e6f). **주 버튼 "내일 아침에"는 홈**으로 간다. 상단 라벨도 홈. 칭찬·점수·퍼센트·연속 일수가 없다. "재만남"이 화면에 없다. 앞 두 행만 "켜짐" 알약, 또 만날 글자 행에는 알약이 없다. **또 만날 글자는 F12 가 진하게 보여 주는 바로 그 글자·그 개수**다(둘을 열어 맞춘다). 한자 n 은 오늘 끝낸 카드 수와 같다. 시간은 마지막 줄 한 곳에만 있다(11차: 그 줄이 아직 없다) |
| 설정 전체 내보내기 `/api/export` | 행 탭 | 파일이 내려온다(`anchor-export-YYYY-MM-DD.json`, `attachment`), 화면은 설정에 남는다. JSON 최상위 format·version·exported_at·user_id·data. data 는 users·inputs·nodes·edges·user_node_state·cards·chunks·recordings·encounters. 표마다 행 수가 DB 의 내 것과 같고 남의 행 0, 공용 노드(user_id 없음) 0. 로그인 없이 치면 307 → `/`. 자료 본문·읽기(meta.readings)·추측·녹음 피치(148점)·노드 상태 source 가 들어 있다 |
| 설정 `/settings` | 각 행 | 라벨 "언어" 아래 언어 행(켜짐/꺼짐 알약, 탭하면 켜기·끄기)과 **"언어 추가" 행. 탭하면 O02a 추가 모드**로 간다. 라벨 "계정" 아래 다섯 행이 이 순서다: **이메일 · 로그아웃 · 음성 보관 기간 · 전체 내보내기 · 계정 삭제.** 음성 보관 기간 행에는 기본값 **30일**이 화면에 보인다(`docs/SPEC.md` 6.6). 주 버튼 "홈으로" 하나, 이탈은 없다 |
| 계정 삭제 확인 | 아니, 그만둘래 · "삭제" 입력 · 지울게 | 한 줄은 "지우면 백업 사본까지 함께 지워져. 되돌릴 수 없어." **검은 주 버튼은 "아니, 그만둘래"** 이고 누르면 설정으로 돌아가며 **아무것도 지워지지 않는다**(자료·언어·로그인이 그대로다). 지우는 것은 그 아래 **회색 링크 "지울게"** 이고 **"삭제"를 입력하기 전에는 꺼져 있다.** 이 링크는 이탈이 아니라 이 화면이 하는 일이므로 이탈 1개 규칙과 따로 센다 |
| F13 못 한 말 `/talk` | 한 줄 입력 · 내가 먼저 해 볼게 | 영어를 켠 뒤에만 열린다. 라벨 "못 한 말", 상단 라벨 → 홈. 빈 칸이면 주 버튼이 꺼져 있다. 입력 뒤 주 버튼 "내가 먼저 해 볼게" → F17 (FLOW 1′장) |
| F17 추측 `/talk/[id]/guess` | 한 줄 입력 · 이제 확인 | 맨 위 "내가 하려던 말", 그 아래 "영어로 뭐라고 할 것 같아?" 입력. **답(영어 문장)이 이 화면에 없다.** 빈 칸이면 주 버튼 꺼짐. "이제 확인" → F14. 상단 라벨 → F13. 쓴 말은 그대로 저장된다. **HTML·RSC 페이로드·네트워크·플레이스홀더 어디에도 영어 문장이 없고, 그 시점 DB 행의 `text` 가 비어 있다. `/talk/[id]` 를 직접 열어도(새로고침·뒤로·다른 탭) F17 로 돌아온다**(FLOW 1′장·4장) |
| F14 덩어리 `/talk/[id]` | 듣기 · 말하기 · 됐어 | **맨 위 카드가 "내가 하려던 말"이고 F13 에 쓴 한 줄 그대로다.** 그 아래 **내 추측(F17)과 영어 문장이 나란히** — "내가 쓴 것" / "이 말" 두 줄의 글자 크기·굵기·색이 DOM 값으로 같고, 정오·점수·%·빨간 줄·취소선이 없다(FLOW 4장 "추측은 채점하지 않는다"). 엉뚱한 추측을 써도 화면이 판정하지 않는다. 판정 칸은 스키마에 없다. 원어민 음성 키가 없으면 F14a: 원어민 선 없음, 범례 "나, n회차"뿐, "아직 견줄 원어민 소리가 없어…", 수치 없음. 강조는 덩어리만. 곡선에 범례 "원어민 / 나"가 있고 **회차는 실제 녹음 횟수다**(화면을 다시 열어도 0 부터 세지 않는다). 상단 라벨 → F13. "됐어" → F15 |
| O02a 추가 모드 `/onboarding/languages` | 켜진 언어 행 · 다음 | 켠 언어는 "켜짐" 고정이고 탭해도 바뀌지 않는다. 새로 고른 언어만 "다음"으로 그 언어의 F02. 이탈은 "홈으로" |
| not-found | 홈으로 | 한국어 1장("이 페이지는 없어."), 버튼 "홈으로" 하나. 이탈 없음 |
| 크론 (프로덕션) | — | `vercel.json` 이 매일 `/api/cron/backup`(18:17 UTC)·`/api/cron/voice`(18:47 UTC)를 부른다. 로컬에서는 못 돌린다. PM 이 확인한 첫 실행: 2026-09-20 백업 18:17:45 200, 녹음 정리 18:47:21 200, 오류 0 |
| error | 홈으로 · 다시 시도 | 한국어 1장, "잠깐 문제가 생겼어. 방금 한 건 저장돼 있어." 주 버튼 "홈으로" 하나. **이탈 "다시 시도"가 있고** 누르면 오류가 난 화면을 다시 그린다 |

"이어서" 알약 · F12 상단 라벨 · F15 주 버튼 · 설정 다섯 행 · 언어 추가 행 · 계정 삭제 확인 · error 의 "다시 시도"는 `docs/FLOW.md` 1′장, 되돌릴 수 없는 일의 버튼 규칙은 4장이 근거다.

## 6. 등급

하나만 고른다. 같은 뿌리에서 여러 화면이 깨지면 뿌리 하나로 센다.

| 등급 | 무엇 | 예 |
| --- | --- | --- |
| **막힘** | 경로가 끊긴다. 다음으로 갈 수 없거나, 돌아갈 수 없거나, 틀린 데이터가 저장된다 | 주 버튼이 404 로 간다. 뒤로 갈 길이 없다. 안 배운 한자가 큐에서 빠진다 |
| **헷갈림** | 갈 수는 있는데 뜻이 잘못 전달되거나 사용자가 잘못 고르게 한다 | 안 한 일이 통과로 기록된다. 두 답 중 하나가 정답처럼 보인다. 화면이 폐기된 개념을 말한다 |
| **다듬기** | 뜻은 통하는데 기준과 다르다 | 어휘가 규칙 밖이다. 문구가 사실과 다르다. 탭 영역이 44px 아래다 |

**막힘 0건**이 첫 방문 경로의 합격선이다(`docs/TEAM.md` 3장).

등급은 사용자에게 무슨 일이 일어나는지로 정한다. 고치기 쉬운지 어려운지로 정하지 않는다.

## 7. 보내는 곳

| 무엇 | 어디로 |
| --- | --- |
| 코드가 FLOW 와 다르다 | Anchor · 개발 |
| FLOW 가 스스로와 어긋나거나 빠져 있다 | Anchor · PM |
| 판정 결과 목록 | Anchor · PM |
| 구현이 참고 HTML 과는 같은데 둘 다 FLOW 와 다르다, 또는 참고 HTML 이 FLOW 와 다르다 | Anchor · PM (기준·디자인 중 어느 쪽을 고칠지 PM 이 정한다. 개발에는 보내지 않는다) |

항목마다 이렇게 적는다: 라우트·화면 ID, 실제로 일어난 것, FLOW 의 어느 절과 어긋나는지, 코드인지 기준 문서인지 내 판단, 등급.

## 8. 도구

| 무엇 | 어떻게 |
| --- | --- |
| 참고 HTML 대조 | `pnpm design:check <화면 ID> <URL>`. `.design-check/` 에 참고·앱·차이 이미지가 남는다. 글리프 차이로 1~3%는 정상이고, 그 위는 레이아웃 차이를 의심한다 |
| 화면 보기 | Playwright 로 390×844 전체 화면을 찍는다 |
| 4장 자동 검사 | 같은 세션에서 글자 크기·글자색·탭 영역·제목 태그·대체 텍스트·ruby·주 버튼 개수·링크 목적지를 DOM 에서 센다. 미리보기 라우트 17개를 한 번에 돈다 |
| 참고와 구현 한꺼번에 대조 | `pnpm design:diff`. 미리보기 서버(`ANCHOR_DESIGN_PREVIEW=1`)로만 돈다 — 실계정 서버에 돌리면 O01 빼고 21장이 "열리지 않음(→ 로그인)"으로 나온다. 22장에 45~52초. 판정이 아니라 볼 차례다. 읽는 법은 `design/SCREENS.md` "참고와 구현의 차이 훑기" |
| 참고 HTML 규칙 검사 | `pnpm design:lint`. `<a>` 의 높이만 재므로 `<span>` 선택 칩과 링크 폭은 못 잡는다 |

자동 검사는 4장의 일부만 덮는다. 경로가 끊기는지, 문구가 사실인지, 무엇이 저장되는지는 사람이 눌러 봐야 안다.

## 9. 판정 기록

라운드마다 한 줄씩 남긴다. 실패는 아래에 항목별로 적는다.

| 라운드 | 날짜 | 대상 커밋 | 막힘 | 헷갈림 | 다듬기 | 비고 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-20 | 549b2b1 | 2 | 8 | 8 | 첫 방문 경로 미구현. 온보딩 6화면만 봄 |
| 2 | 2026-09-20 | 31feba8 | 0 | 2 | 4 | 1차(미리보기). 문구·모양·링크·404·ruby 가림만. 상태가 바뀌는 항목은 2차로. PM 판정 반영 뒤 숫자 |
| 3 | 2026-09-20 | 49a63a8 | 1 | 2 | 6 | 2차(실계정, 로컬 Postgres + 시험 로그인). 첫 방문 경로 17탭 끝까지. 막힘 1 은 갈래(언어 둘) |
| 4 | 2026-09-20 | 505e9eb | 0 | 1 | 1 | 3차 9건 재확인(7 닫힘, 2 절반) + 후리가나 + ruby 금지 자리 + 영어 예시. 기준 질문 1 |
| 5 | 2026-09-20 | 376d1fd | 0 | 0 | 2 | 4차 판정 3건 재확인(전부 닫힘) + 주 경로 회귀 17탭. 다듬기 둘은 대체 텍스트의 조사, 발판 없을 때의 자료 이름 |
| 6 | 2026-09-20 | e2a0ece | 0 | 0 | 1 | 5차 판정 2건 + 기획 발견 1건 재확인(전부 닫힘) + 주 경로 회귀 17탭. 다듬기 하나는 F11 회색 링크의 자료 이름 |
| 7 | 2026-09-20 | 26ffd0a | 0 | 0 | 0 | 언어별 예시 이름·로/으로·F11 링크 전부 닫힘 + 주 경로 회귀 17탭. F17 은 미구현(결함 아님) |
| 8 | 2026-09-20 | ad4ce1b | 0 | 0 | 0 | F17 첫 검증 — 답 안 샘·건너뛰기 없음·채점 흔적 없음·F14a·저장 전부 통과 + 주 경로 회귀 17탭. PM 질문 2 |
| 9 | 2026-09-20 | db830a0 | 0 | 1 | 0 | 추측 한 번·곡선 세션 넘김·쓰다 만 글 전부 통과. 가짜 마이크로 가나 인식 0개·실제 녹음까지 봄. 헷갈림 1 은 F10 다시 열기 |
| 10 | 2026-09-20 | 5fae641 | 0 | 1 | 2 | F12 재만남 첫 검증 — 읽기 안 샘·틴트와 빈칸 한 사실·개수 줄 통과. 다듬기 둘은 틴트 배경이 안 칠해짐, h1 없음. F10 9차 건 닫힘. 추가: 손댄 적 없는 경로(inferred 등)는 F12 에서 새 것 — 통과. 헷갈림 하나는 같은 넷을 F03 이 「아는 것」으로 부름 |
| 11 | 2026-09-20 | 57a0f37 | 1 | 2 | 2 | MVP 5단계 닫힘 라운드. F15 첫 검증·F12 재고침·design:diff 첫 실행·주 경로 18탭(가짜 마이크)·영어 축. 막힘 하나는 카드를 다 끝내면 홈에 못 돌아가는 F15 루프. 헷갈림은 Scene5 착지 낱말이 안 만난 한자의 읽기·한국어를 주는 것, 다 만난 뒤에도 "타협"이 안 보이는 것. 다듬기는 F12 틴트가 낱말 안에서 끊김, F03 한자 크기. 10차 다듬기 둘 닫힘 |
| 12 | 2026-09-21 | 219c854 | 0 | 1 | 0 | 11차 다섯 건 모두 닫힘 확인(홈 고리·Scene5 착지·"타협"·틴트 묶음·F03 30px). 전체 내보내기 첫 검증 — 파일 나오고 내 것만, 로그인 없이는 튕김. 헷갈림 하나는 내보낸 노드 상태가 uuid 만 들고 있어 파일만으로는 어느 한자인지 모른다. design:diff 재실행에서 Scene5 순위가 5→12 로 내려감 |

### 라운드 1 (2026-09-20, 549b2b1)

FLOW 가 준비 단계를 버린 직후라, 검사 대상이 대부분 폐기 예정 화면이었다. 실제 계정이 아니라 `ANCHOR_DESIGN_PREVIEW=1` 로 돌았다.

| 등급 | 항목 | FLOW 근거 |
| --- | --- | --- |
| 막힘 | `/inputs/new` 가 404 | 1장 3 |
| 막힘 | 상단 라벨이 링크가 아니라 어느 화면에서도 뒤로 갈 수 없다 | 4장 |
| 헷갈림 | 마이크가 진입 즉시 켜진다 | 1장 4′ |
| 헷갈림 | 마이크 미지원·거부가 통과로 저장된다 | 1장 4′ |
| 헷갈림 | 0개 인식 상태에서 "다 읽었어"가 눌린다 | 1장 4′ |
| 헷갈림 | `/onboarding/purpose` 가 아직 큐에 있다 | 5장 |
| 헷갈림 | 씨앗 40장 두 화면이 아직 있다 | 5장 |
| 헷갈림 | 언어 순서가 코드에 고정이다 | 4장 |
| 헷갈림 | 전 화면에 제목 태그가 없다 | CLAUDE.md 디자인 시스템 (라운드 1 뒤 들어옴) |
| 헷갈림 | 답변 2버튼 중 검은 쪽이 정답처럼 보인다 | CLAUDE.md 디자인 시스템 (화면당 주 버튼 1개) · 4장 (고르는 것은 선택 칩) |
| 다듬기 | `not-found` · `error` 가 영문 기본 페이지다 | 4장 |
| 다듬기 | 알약 어휘가 켜짐 / 할래 / 나중에 다 | 4장 |
| 다듬기 | "그래프에 심는 중"이 화면에 보인다 | 4장 |
| 다듬기 | 상단에 "일본어 2 / 3" 단계 표시가 있다 | 5장 |
| 다듬기 | O02a 부제가 폐기된 준비 단계를 말한다 | 5장 |
| 다듬기 | O02a 이탈이 설정으로 간다 | 1장 2 |
| 다듬기 | 로고에 대체 텍스트가 없다 | CLAUDE.md 디자인 시스템 (라운드 1 뒤 들어옴) |
| 다듬기 | 상황 화면 알약의 탭 영역이 43px 다 | CLAUDE.md 디자인 시스템 (라운드 1 뒤 들어옴) |

기준 문서 쪽으로 올린 것은 다섯이다. 모두 닫혔다.

| 무엇 | 결과 |
| --- | --- |
| 가나 잠금이 첫 5분과 부딪힌다 | `docs/FLOW.md` 4장. 잠그지 않는다 |
| 알약이 상태와 답변 두 가지로 쓰인다 | `docs/FLOW.md` 4장. 알약과 선택 칩을 가른다 |
| 추측 전에 ruby 를 붙일지 | `docs/FLOW.md` 4장 · `CLAUDE.md`. 추측 전까지 가린다 |
| 탭 영역 44px | `CLAUDE.md` 디자인 시스템 |
| "알아 / 몰라"가 세 층 중 어느 층에 쓰이는지 | `docs/SPEC.md` 6.5. 판정 결과는 `knows_meaning`, `knows_sound`(한국어 한자어 소리)는 한국어 화자 기본값 true |

### 라운드 2 — 1차, 미리보기 (2026-09-20, 31feba8)

시험 로그인이 아직 없어 `ANCHOR_DESIGN_PREVIEW=1` 로 돌았다. 본 것은 문구·모양·링크 목적지·404·읽기 가림뿐이다. 무엇이 저장되는지, 큐에서 빠지는지, 가나 확인이 한 번만 뜨는지, 5분·버튼 17번은 **판정하지 않았다**(2차). `/settings` · `/settings/delete` 는 미리보기 분기가 없어 로그인으로 튕기므로 코드를 읽어 판정했고, `error` 는 미리보기로 일으킬 수 없어 코드로만 봤다.

통과한 것: 404 는 첫 방문 경로 위에서 0건. 글자색·글자 크기·폰트·주 버튼 1개·844px 넘침·대체 텍스트는 17화면 전부 통과. F02 예시 행·O03·O03b(두 문구)·not-found·error 문구는 참고 HTML 또는 FLOW 그대로. 상단 라벨은 카드 → F04, F04·F11 → 자료, 자료·설정·O03b → 홈. Scene4·Scene5·F10 에서 읽기가 보인다. F03 알아/몰라는 선택 칩(흰 배경 + 테두리)이고 두 묶음으로 갈린다. `pnpm design:lint` 는 참고 38장 통과.

| 등급 | 항목 | FLOW 근거 | 보낸 곳 |
| --- | --- | --- | --- |
| 헷갈림 | 17화면 전부 `h1` 이 없다. 제목이 `div` 다(`Title` 컴포넌트). 라운드 1 과 같은 건 | CLAUDE.md 디자인 시스템 | 개발 |
| 헷갈림 | 계정 삭제 확인(`/settings/delete`): 검은 주 버튼이 실행("계정 삭제")이고, 돌아가는 버튼 "아니, 그만둘래"와 회색 링크 "지울게"가 없다. 문구도 "정말 지울까? / 모든 데이터가 지워지고 되돌릴 수 없어. 백업 사본도 함께 지워져. 계속하려면 아래에 삭제 라고 써."로 FLOW 와 다르다. 참고 F16a 는 버튼 배치가 FLOW 대로다 | 4장 · 1′장 | 개발 |
| 다듬기 | F04 문장(トヨタとNTT、協力して)과 Scene2 부품(十·力)에 ruby 가 하나도 없다. 참고 F04·Scene2 는 빈칸 ruby(폭 같음)를 단다. 빈칸이 없으니 폭 규칙은 판정 불가 | 4장 · CLAUDE.md | 개발 |
| 다듬기 | 설정 음성 보관 기간 행이 "30일"을 알약으로 보인다. 알약 어휘는 켜짐/꺼짐/이어서뿐. 참고 F16 은 부제("녹음 원본은 30일 뒤 지움")에 쓴다 | 4장 | 개발 |
| 다듬기 | 탭 영역 44px 미만: F03 알아/몰라 칩 50×33, O03·O03b 상단 라벨 "가나" 폭 26. 참고 HTML 도 같다(칩 52×32). `design:lint` 가 `<a>` 높이만 재서 못 잡는다 | CLAUDE.md 디자인 시스템 | PM |
| 다듬기 | O03·O03b 상단 라벨이 홈으로 간다. 한 단계 위는 F03(그 자료)이고 참고 O03 은 F03 이다 | 4장 (PM 판정) | PM → 개발 |

`/today/done`(F15)과 `/inputs/[id]/read`(F12)는 404 인데 결함이 아니다. `docs/SPEC.md` 9장 5단계라 지금 범위 밖이고(PM 판정), 어느 화면에도 그 주소로 가는 링크가 없는 것을 `src` 전체에서 확인했다. 링크가 생기는 순간 막다른 길이라 막힘이다. O02a 언어 고르기의 알약은 결함으로 올렸다가 내렸다. 언어는 켜고 끄는 것이라 FLOW 1장 2·1′장 대로 켜짐/꺼짐 알약이 맞다(PM 판정).

라운드 1 의 18건 중 이번에 다시 보인 것은 제목 태그 하나다. 나머지는 폐기 화면과 함께 사라졌거나 고쳐졌다(`/inputs/new` 404, 상단 라벨 링크, 마이크 즉시 켜짐, 알약 어휘, 단계 표시, not-found·error, 로고 대체 텍스트).

**참고 HTML 이 FLOW 와 다른 곳** — 구현이 아니라 참고 쪽 문제라 PM 에 보낸다. 구현(실계정 분기)은 아래 전부 FLOW 를 따른다.

| 화면 | 참고 HTML | FLOW |
| --- | --- | --- |
| F11 | 검은 주 버튼 "아침 기사로 돌아가기" → F03, 회색 "다음 카드 助" → F04 | 주 버튼 "다음 카드 助" → F04, 이탈 "자료로 돌아가기" → F03 (1장 8). 미리보기 분기도 참고를 따라 반대로 그린다 |
| F12 | 상단 라벨 → F01, "읽기 끝" → F13 | 상단 라벨 → F03(그 자료), "읽기 끝" → 홈 (1′장) |
| F01 | 자료 행에 "이어서" 알약 없음. 회색 링크가 "바로 시작" → F04 와 "설정" 둘 | 카드가 남은 행은 "이어서" 알약, 이탈은 설정 하나 (1′장·4장) |
| X02 | "뭔가 잘못됐어 / 넣던 것은 남아 있어. 잠깐 뒤에 다시 해 봐." 이탈 없음 | "잠깐 문제가 생겼어. 방금 한 건 저장돼 있어." 이탈 "다시 시도" (1′장) |
| F16a | "삭제" 입력 칸이 없다. 문구 "계정을 지우면 되돌릴 수 없어 / 넣은 자료도, 추측도, 녹음도, 백업 사본까지 같이 지워져." | 링크는 "삭제"를 입력해야 켜진다. 한 줄 "지우면 백업 사본까지 함께 지워져. 되돌릴 수 없어." (1′장) |
| F04 · Scene2 | 協과 力 둘 다 빈칸 ruby | 빈칸은 그 카드의 대상 한자(協)만 (4장) |

PM 판정: 위 여섯은 참고가 낡은 쪽이라 디자인이 고친다. 탭 영역 44px 도 참고부터 고치고 구현이 따라간다(`design:lint` 도 `<span>`·폭까지 재게 넓힌다). O03 상단 라벨은 F03 이다(참고가 맞고 구현이 틀림, 개발에 전달됨).

2차(실계정)에서 볼 것: 3장 전부와 5장의 상태 항목, 그리고 코드로만 본 셋 — 가나 인식 0개가 recheck 로 남는지, `error` 의 "다시 시도", 설정 다섯 행.

### 라운드 3 — 2차, 실계정 (2026-09-20, 49a63a8)

로컬 Postgres(5433) + 시험 로그인으로 초기화된 계정에서 처음부터 돌았다. 헤드리스 Chromium 390×844, 마이크 없음. 세 번 돌았다: 주 경로 한 번, 갈래(언어 둘 · 못 읽겠어 · 전부 알아) 두 번. 계정 초기화는 매번 `POST /api/account/delete` 로 했고 세 번 다 DB 가 비워졌다(users·inputs·cards·chunks·user_node_state 0, account_deletions 1행).

**첫 방문 경로 — 끝까지 돈다.** 로그인부터 F11 까지 **17탭**(FLOW 1장의 목록과 한 번 한 번 같다), 자동 조작으로 23초. 404 0건, 막힘 0건. 매 화면 `h1` 하나, 검은 주 버튼 하나, 상단 라벨이 한 단계 위(F02·F03 → 홈, O03·O03b → F03, F04·F11 → F03, Scene·F10 → F04).

| # | 화면 | 확인한 것 |
| --- | --- | --- |
| 1 | O01 | 쿠키 없이 `/today` → `/?next=%2Ftoday`. 데이터 원칙 2줄 |
| 2 | O02a | "다음"이 꺼져 있다가 일본어 탭 뒤 켜진다. 알약 꺼짐 → 켜짐. → `/inputs/new?lang=ja` |
| 3 | F02 | 예시 행 탭 한 번에 글 상자가 예시 문구(64자)로 차고 주 버튼이 켜진다. inputs 1행, `meta.example: true` |
| 4 | F03 | 한자 21개가 뽑힌다(모두 "아는 소리에서 시작". 예시 문구엔 발판 없는 한자가 없다). "알아" 한 탭 → 그 한자가 "아는 것" 묶음으로 내려가고 제목이 21 → 20. DB `knows_sound t · knows_meaning t · source input`. "몰라" → `knows_meaning f`, 큐에 남는다. 전부 알아 → 제목 "다 아는 한자야", 주 버튼 "홈으로" |
| 4′ | O03 | "읽기 시작" 전 "마이크 꺼짐". 상단 라벨 → F03. 헤드리스라 "읽기 시작" 즉시 O03b 마이크 문구로 가고 `kana.status: recheck, supported: false` — 통과로 남지 않는다. "못 읽겠어" → O03b 예고 문구, `status: module`. 어느 쪽이든 "첫 카드로" → F04. **두 번째 카드에서 다시 묻지 않는다**(F11 → 다음 카드, F03 → 協부터 재진입, 홈 → 이어서 모두 바로 F04) |
| 5 | F04 | "카드 1 / 20"(21 − 알아 1). 대상 한자 협 ruby 빈칸 30px, 두 번째 카드 支(읽기 1글자)도 30px — 폭이 글자 수를 따르지 않는다. 추측 전 `/4` 직접 열기 → `/3` 으로 돌려보낸다 |
| 6 | Scene1~5 | 진행 막대 2/6 → 6/6. Scene3 추측 "힘을 합친다" → Scene4 에 정답·읽기·내 추측·"거의 맞음". DB `guess`·`revealed_at`. Scene5 착지 뒤 `協 knows_meaning t source card`, `landed_at` |
| 7 | F10 | 協力 · きょうりょく, 범례 "원어민 / 나". `/api/tts` 204 → "브라우저 음성이야" 한 줄. "됐어, 다음" → F11 |
| 8 | F11 | "協이 켜졌어", 범례 아는 것 / 오늘 / 다음, 주 버튼 "다음 카드 支", 회색 "아침 기사로 돌아가기" → F03. 다음 카드 → "카드 2 / 20" |
| 9 | F01 | 자료 행 "한자 21개 · 남은 카드 19" + "이어서" 알약 → 열린 카드(F04). 카드를 다 보면 "다 봤어", 알약 없음, 행 → F03. 라벨 "오늘", 시간 없음 |

첫 방문 밖: 설정 계정 다섯 행 순서·30일(부제)·"언어 추가" → O02a 추가 모드(켠 언어 고정, "홈으로") 통과. 계정 삭제 확인: 문구 FLOW 그대로, 검은 버튼 "아니, 그만둘래" → 설정이고 아무것도 안 지워진다(1/1/2 그대로), "지울게"는 "삭제" 입력 전 꺼진 텍스트. 전체 내보내기 200 JSON. 언어 둘(스페인어 → 일본어) → 첫 F02 가 `lang=es`(고른 순서). F13 빈 칸 주 버튼 꺼짐 → 입력 → F14 맨 위 "내가 하려던 말" 카드에 그 줄 그대로, 범례 있음, 상단 라벨 → F13, "됐어" → 홈, 홈에 "못 한 말 1개" 행 → F13.

| 등급 | 항목 | FLOW 근거 | 보낸 곳 |
| --- | --- | --- | --- |
| 막힘 | 언어 둘을 고르면 두 번째 언어로 갈 길이 없다. 홈은 첫 언어의 "첫 자료 넣기"만 있고, F02 에 언어 바꾸는 자리가 없고, 설정의 "언어 추가"는 켠 언어를 고정한다. `/inputs/new?lang=ja` 를 손으로 쳐야 닿는다 | 1장 2 "나머지는 홈에서 이어서" | 개발 |
| 헷갈림 | F10·F14 에서 마이크를 거부하면 "마이크를 허용하지 않았어. 듣기만 하고 넘어가도 돼."라면서 "듣기" 버튼도 같이 꺼진다. 듣기만 할 수 없다 | 1장 7 · 4장 마이크 | 개발 |
| 헷갈림 | F02 예시 행 제목이 언어와 무관하게 "日経 기사 한 문장"이다. 스페인어·영어 F02 에서 탭하면 스페인어·영어 문장이 들어간다 | 1장 3 · 4장 예시 자료 | 개발 |
| 다듬기 | 홈 상단 오른쪽 시각이 "PM 11:37"로 영어다(다른 화면은 "오후 11:36"). 홈이 자기 포맷터를 쓴다 | 4장 상단 오른쪽 | 개발 |
| 다듬기 | F03 상단 오른쪽이 시각이 아니라 "한자 21개"다. 참고 F03 은 시각 | 4장 상단 오른쪽 | 개발 |
| 다듬기 | 홈 다음 카드 이유가 "다음은 支. 十를 아니까 가장 가까워." 두 문장이고 다음 것이 앞에 온다. 아는 것이 없을 땐 "다음은 協 (협력). <자료 제목>에서." | 4장 다음 카드 이유 | 개발 |
| 다듬기 | 강조 단어 밖의 한자에 ruby 가 없다: F04 문장의 次世代·車·開発 / 新·基盤·安全·運転, 홈 자료 행 제목(자료 첫 문장) | CLAUDE.md ruby | 개발 |
| 다듬기 | 같은 한자의 앵커 단어가 F03 과 F04 에서 다르다. 支: F03 "지출의 지", F04 "지원은 알아. 支만 모르지" | 1장 4·5 (協 · 협력의 협 → 협력은 알아) | 개발 |
| 다듬기 | F03 을 전부 "알아"로 채우면 제목은 "다 아는 한자야"인데 부제는 "아는 소리로 시작할 수 있는 것부터. 나머지는 나중에." 그대로다 | 1장 4 | 개발 |

구현과 참고가 같이 FLOW 와 다른 것 하나 — PM: F02 상단 오른쪽이 시각이 아니라 언어 이름("일본어")이다. 참고 F02 도 같다(4장 "상단 오른쪽은 시각").

FLOW 에 없어 PM 에 묻는 것 하나: **그래프와 홈의 "아는 것" 자리에 사용자가 판정한 적 없는 부품이 들어간다.** 協 카드 뒤 F11 은 "十를 아니까 支·次가 가장 가까워"라 하고 홈도 "十를 아니까"라 하는데, 十은 알아/몰라로 판정된 적도 `user_node_state` 에 있지도 않다(있는 것은 力·協 둘). SPEC 6.5 "맞추면 인접 노드까지 아마 아는 것"으로 보면 맞고, 판정된 것만 아는 것으로 보면 틀리다.

**회차 — 통과** (main d527993 뒤, 같은 계정에서 추가로 봤다). 녹음은 헤드리스라 `/api/recordings` 로 넣었다(카드 5회 · 덩어리 3회, 전부 200, DB `attempt` 1..5 / 1..3). 넣기 전 범례는 "원어민 / 나", 넣은 뒤 홈으로 나갔다 다시 들어오면 F10 "나, 5회차" · F14 "나, 3회차", 새로고침해도 같다. 한 번 더 넣으면 6회차·4회차로 이어진다. 없는 카드 id 로는 404. 범례는 DOM 주석 때문에 문자열 검색으로는 못 찾고 `textContent` 로 비교했다.

미검증 — 마이크 있는 브라우저나 키가 있어야 한다: O03 인식 0개에서 "다 읽었어"(코드로는 `recognized < 4` → recheck), 마이크로 실제 녹음해 저장되는지(API 는 통과), 원어민 곡선, `error` 화면의 "다시 시도".

### 라운드 4 — 3차 결함 재확인 · 후리가나 (2026-09-20, 505e9eb)

실계정(로컬 Postgres + 시험 로그인), 헤드리스 390×844, 마이크·API 키 없음. 주 경로를 처음부터 다시 돌았고(로그인 → F11 **17탭**, 회귀 없음), 갈래는 언어 셋(스페인어 → 일본어 → 영어)으로 한 번 더 돌았다.

**3차의 9건** — 번호는 개발에 보낸 목록 그대로.

| # | 3차 항목 | 이번에 누른 것 → 보인 것 | 결과 |
| --- | --- | --- | --- |
| 1 | 둘째 언어로 갈 길 없음 | 스페인어·일본어·영어를 켜고 영어 자료 1개를 넣은 뒤 홈 → 행 "스페인어 자료 넣기 · 영어 어근에서 시작" → `/inputs/new?lang=es`, "일본어 자료 넣기 · 한국어 한자음에서 시작" → `?lang=ja`. 고른 순서대로. **그런데 자료가 0개인 빈 상태**(세 언어 켜고 아무것도 안 넣음)에서는 홈에 "첫 자료 넣기"(→ 첫 언어 es) 하나뿐이고 일본어·영어 행이 없다 | 절반. 빈 상태는 기준 충돌(아래) |
| 2 | 마이크 거부 뒤 듣기도 꺼짐 | F10 에서 말하기 → "마이크를 허용하지 않았어. 듣기만 하고 넘어가도 돼." → "듣기" 버튼이 켜져 있다 | 닫힘 |
| 3 | 예시 행 제목이 언어 무관 | F02 예시 행: 일본어 "日経 기사 한 문장", 스페인어 "안내문 한 문장", 영어 "회의 노트 한 문장". 탭하면 각 언어 본문 | 닫힘 |
| 4 | 홈 시각 "PM" | 홈 오른쪽 "오전 12:41" | 닫힘 |
| 5 | F03 오른쪽 "한자 21개" | F03 오른쪽 "오전 12:41" | 닫힘 |
| 6 | 다음 카드 이유 순서 | 홈 "十를 아니까 支가 가장 가까워." 한 문장, 아는 것이 앞. 그러나 조사가 틀렸다 — 十은 "십"이라 "十을"이어야 한다(nodes 의 十 kanji 행에 ko_sound 십이 있다). F11 도 "十를 아니까 …" | 절반. 조사는 아래 표 |
| 7 | 강조 밖 한자 ruby 없음 | 아래 "후리가나" | 키 없는 환경의 정상 동작으로 닫힘 |
| 8 | F03·F04 앵커 단어 불일치 | F03 "支 지출의 지" → 두 번째 카드 F04 "지출은 알아. 支만 모르지." | 닫힘 |
| 9 | 전부 알아 뒤 부제 | F03 21개 전부 알아 → "다 아는 한자야 / 여기서 새로 배울 건 없어. 다른 자료를 넣어 봐." | 닫힘 |

**후리가나** (`e5be068`). 키가 없으니 카드 payload 에 `source.readings` 가 없고, F04 문장 "協力して次世代の車を開発"에는 대상 단어의 점선 빈칸(協·力 각 30px)만 있고 다른 한자에 ruby 가 하나도 없다 — 지어낸 읽기 0. 렌더링은 DB 에 맞는 읽기(`["きょうりょく","じせだい","くるま","かいはつ"]`)를 직접 넣고 봤다: 協力 은 그대로 빈칸, 次世代→じせだい · 車→くるま · 開発→かいはつ 가 붙는다. 한 덩어리를 빈 문자열로 바꾸면 그 덩어리만 ruby 없이 나온다. Scene1·Scene4 는 영향 없다. `alignReadings` 를 아홉 경우로 돌렸다: 개수 모자람·넘침·전부 빈 값·한자 없는 문장 → null(문장 통째로 ruby 없음), 한자 섞임·가타카나·로마자·터무니없이 긴 것·하나만 빈 값 → 그 덩어리만 빈 문자열. **읽기가 맞게 붙는지는 미검증** — 키가 있어야 한다.

**ruby 를 달면 안 되는 자리**: 홈 자료 행 제목(トヨタとNTT、協力して次世代の車を開発), 홈 다음 카드 이유 한 줄, F11 제목·설명·그래프, F03 한자 행 — 네 곳 다 `<ruby>` 0.

**영어 예시 · F02 상단**: 영어 예시 본문 "Let's review the deployment schedule before the retrospective. I want to inspect the numbers first." 에 "push this to" 없음. F13 자리표시자는 "이건 다음 스프린트로 미루죠". F02 왼쪽 라벨 "일본어 자료 넣기"/"영어 자료 넣기"/"스페인어 자료 넣기"(→ 홈), 오른쪽 시각. 영어 예시를 뽑으면 F03 대신 "자료는 저장됐어 / 영어 뽑기는 다음 단계야" 1장 — 영어 뽑기는 아직 범위 밖이라 사실만 적는다.

| 등급 | 항목 | 누른 것 → 보인 것 | FLOW 근거 |
| --- | --- | --- | --- |
| 헷갈림 | F11 그래프에 力이 "아는 것"과 "다음"에 동시에 있다 | 協 카드를 끝내고 "됐어, 다음" → F11. 위(아는 것) 동그라미 十·力, 아래(다음) 동그라미 支·力. 설명 "十를 아니까 支(지출)·力(협력)이 가장 가까워." 사용자는 力을 알아/몰라로 판정한 적이 없다. 3차 질문(부품 = 아는 것?)과 같은 뿌리 | 4장 다음 카드 이유 · SPEC 6.5 |
| 다듬기 | 조사가 부품 뒤에서 틀린다 | 홈 "十를 아니까", F11 "十를 아니까". 十은 십 → "十을". 力(협력)이·支가 는 맞다 | 4장 예문 "力을 아니까" |

기준 문서 질문 — PM: **언어를 둘 이상 켜고 자료가 0개일 때** 홈은 무엇을 보여 주나. 1장 2 "나머지는 홈에서 이어서"와 1′장 빈 상태 "첫 자료 넣기 버튼 하나"가 부딪힌다. 지금은 뒤쪽을 따라 첫 언어만 열려 있다.

### 라운드 5 — 4차 판정 3건 재확인 (2026-09-20, 376d1fd)

실계정(로컬 Postgres + 시험 로그인), 헤드리스 390×844, 마이크·API 키 없음. 계정 다섯 개를 새로 만들어 돌았다(발판 없음 · 주 경로 · 언어 하나 빈 홈 · 언어 셋 빈 홈 · 추측 없이 끝낸 카드). 주 경로는 로그인 → F11 **17탭**, 회귀 없음.

**1) 「아는 것」에는 판정된 노드만 — 닫힘.**

| 누른 것 | 보인 것 | DB |
| --- | --- | --- |
| F03 에서 力 "알아", 次 "몰라" → 協 카드를 추측 "힘을 합친다"로 끝내고 "됐어, 다음" → F11 | 위(아는 것) 力 하나, 가운데 協, 아래(다음) 支·次. 十 은 어디에도 없고 力 은 위에만 있다 | `力 input` · `協 card` · `次 input(false)` |
| 이어서 支 카드를 추측 없이 끝내고 "다음 한자로" → F11 | 위 協·力, 가운데 支, 아래 次·世. 겹치는 노드 없음 | `支 card` |
| 새 계정, 아무것도 판정 않고 協 카드만 추측 없이 끝냄 → F11 | 위에 아무것도 없고 가운데 協, 아래 支·力. 보여 주기만 한 부품 十·力 이 위에 오지 않는다 | `協 card` 뿐 |

판정해서 들어간 것(`source: input`)과 카드로 들어간 것(`source: card`)은 둘 다 「아는 것」이고, 동사만 다르다(아래).

**2) 이유 한 줄 — 닫힘.** 괄호 없음. 동사는 발판을 얻은 방법을 따른다.

| 상황 | 홈 | F11 |
| --- | --- | --- |
| 力 알아만 (카드 없음) | 力을 아니까 協이 가장 가까워. | — |
| 協 카드 추측 맞음 | 協을 맞혔으니 支가 가장 가까워. | 協을 맞혔으니 支·次가 가장 가까워. 다음 카드는 이 둘 중 하나. |
| 協 카드 추측 없이 끝냄 | 協을 켰으니 支가 가장 가까워. | 協을 켰으니 支·力이 가장 가까워. … |
| 발판 없음 (판정 0, 카드 0) | 다음은 協. トヨタとNTT、協力して에서. | — |
| 支 를 끝냈는데 다음 후보가 부품을 안 나눔 | 다음은 次. トヨタとNTT、協力して에서. | 次·世가 남았어. 다음 카드는 이 둘 중 하나. |

**3) 조사 — 닫힘, 한 자리 남음.** 화면에서 協을·協이·力을·支가·次·世가·支·力이 전부 맞다. `withParticle` 을 소리 여덟 개로 따로 돌렸다: 十(십)을·이·은, 支(지)를·가·는, 協(협)을, 力(력)을, 次·開·車(차·개·차)를, 소리 없는 十 은 "十를"로 떨어진다. 한자 뒤에 조사가 붙는 자리를 `src` 전체에서 훑었다 — 홈 이유·F11 이유·F11 제목·Scene5 제목 넷은 `withParticle`, F04 "협력은 알아"는 순한국어 단어라 `eunNeun`. **안 거친 자리 하나**: F11 그래프 SVG 의 대체 텍스트(`aria-label`)가 "{한자}이 켜지고 {후보}가 다음으로 뜬 그래프"로 조사를 박아 둬서 "支이 켜지고 次·世가", "協이 켜지고 支·力가"로 읽힌다. 눈에는 안 보이고 스크린 리더에만 들린다.

**4) 빈 홈 — 닫힘.** 일본어 하나만 켜고 자료 0개 → "첫 자료 넣기" 버튼 하나(→ `lang=ja`), 행 없음. 스페인어 → 일본어 → 영어 순으로 켜고 자료 0개 → "오늘" 카드 안에 "스페인어 자료 넣기 · 영어 어근에서 시작" / "일본어 자료 넣기 · 한국어 한자음에서 시작" / "영어 자료 넣기 · 업무 어휘에서 시작" 세 행이 고른 순서로, 검은 주 버튼 "자료 넣기" 하나(→ `lang=es`). 세 행을 각각 탭하면 `lang=es`·`ja`·`en` 의 F02 로 가고 왼쪽 라벨이 "스페인어/일본어/영어 자료 넣기"다. 일본어 자료를 하나 넣은 뒤 홈: 자료 행 + 스페인어·영어 행 + "못 한 말" 행, 주 버튼 여전히 하나 — 4차 홈과 같은 뼈대.

| 등급 | 항목 | 누른 것 → 보인 것 | FLOW 근거 |
| --- | --- | --- | --- |
| 다듬기 | 그래프 대체 텍스트의 조사가 고정이다 | 支 카드를 끝내고 F11 → 그래프 `aria-label` "支이 켜지고 次·世가 다음으로 뜬 그래프". 화면 제목은 "支가 켜졌어"로 맞다. `src/app/graph/page.tsx` 의 SVG 한 곳 | 4장 조사 · CLAUDE.md 대체 텍스트 |
| 다듬기 | 발판이 없을 때 자료 이름이 문장 중간에서 잘린다 | 판정 0·카드 0 인 홈 → "다음은 協. トヨタとNTT、協力して에서." 제목 앞 12자에 "에서"를 붙여 단어 중간(協力して)에서 끊긴다. 같은 자료를 F04 는 "오늘 아침 기사에서"라 부른다 | 4장 다음 카드 이유 · 1장 5 |

사실 하나(등급 없음): `src/lib/kanji/card-content.ts` 의 Scene3 질문 문안 "{부품 뜻}이 모이면"도 조사가 고정이다. 순한국어 자리라 `withParticle` 대상은 아니지만, 부품 뜻이 받침 없이 끝나면 "…해이 모이면"이 된다. 지금 화면의 協 카드는 손 문안("열 사람이 힘을 모으면")이라 안 보였다.

### 라운드 6 — 5차 판정 재확인 · 자료 이름 (2026-09-20, e2a0ece)

실계정, 헤드리스 390×844, 마이크·API 키 없음. 계정 넷(주 경로 · Scene3 넷 · 제목 여섯 · 사전 조합 카드 둘). 주 경로 로그인 → F11 **17탭**, 회귀 없음.

**1) 그래프 대체 텍스트의 조사 (5차 A) — 닫힘.** DOM 의 `aria-label` 을 읽었다. 協 을 맞힌 뒤 F11: "協이 켜지고 支·力이 다음으로 뜬 그래프". 支 를 끝냈는데 후보가 없을 때: "支가 켜진 그래프. 다음 후보는 없어." 5차의 "支이"·"支·力가"가 없다.

**2) Scene3 이음말·조사 (기획 발견) — 닫힘.** 예시 기사의 한자는 전부 손 문안이라(次 "잠시 멈추고 그다음이면", 基 "무언가를 받치는 흙이면", 安 "집 안에 사람이 편히 있으면", 支 "손으로 가지를 받치면") 사전 조합이 화면에 안 나온다. 손 문안이 없는 한자로 "休日に主人と会う。"를 넣어 봤다: 休(사람 인 + 나무 목) → "사람과 나무가 모이면", 主(돼지해머리 두 + 흙 토) → "돼지해머리와 흙이 모이면". 과/와 는 앞말, 이/가 는 끝말을 따른다. `joinWaGwa` 단독으로도 열과 힘이 · 나무와 힘이 · 열과 힘과 입이 · 그와 흙이 · 열과 또가.

**3) 자료 이름 (5차 B) — 닫힘, 한 자리 남음.**

| 자료 | 홈 행 | F03 라벨 | F04 라벨 | 홈 이유 |
| --- | --- | --- | --- | --- |
| 예시 | 아침 기사 | 아침 기사 | 아침 기사에서 | 다음은 協. 아침 기사에서. |
| 11자 協力会社の車を開発する | 그대로 | 그대로 | — | — |
| 12자 協力会社の車を開発します | 그대로 | 그대로 | 協力会社の車を開発します에서 | — |
| 13자 協力会社の車を開発しました | 내 자료 | 내 자료 | 내 자료에서 | — |
| 25자(첫 줄) | 내 자료 | 내 자료 | — | — |
| 여러 줄, 첫 줄 会議メモ | 会議メモ | 会議メモ | — | 다음은 会. 会議メモ에서. |
| 영어 "Sprint review"(13자) | 내 자료 | 내 자료 (안내 1장) | — | — |

DB 의 `title` 은 25자 자료가 "…20자…"로 저장돼 있지만 홈·F03·F04 어디에도 "…"도 20자 제목도 새지 않는다. `inputName` 단독: 11·12자 그대로, 13자 "내 자료", 서로게이트 쌍 12자 그대로·13자 "내 자료", 제목 없음·빈 제목·null 전부 "내 자료". 상단 라벨은 이름만("아침 기사"), 문장은 "아침 기사에서".

| 등급 | 항목 | 누른 것 → 보인 것 | FLOW 근거 |
| --- | --- | --- | --- |
| 다듬기 | F11 회색 링크의 자료 이름이 `inputName` 을 안 거친다 | 13자 자료(홈·F03·F04 는 "내 자료")의 카드를 끝내고 F11 → 회색 링크 "자료로 돌아가기". 예시 자료는 "아침 기사로 돌아가기". `src/app/graph/page.tsx` 가 예시/아님 두 갈래로 직접 쓴다. 같은 자료가 F11 에서만 "자료"다 | 4장 자료 이름 |

사실(등급 없음): F11 에서 후보가 없을 때의 설명 "이 자료의 한자는 다 봤어."는 이름이 아니라 일반 문장이다. 참고 HTML F03("이 기사에서")·F04("오늘 아침 기사에서")는 아직 옛 문구다 — 개발 커밋도 적어 뒀고, 디자인이 고칠 자리다.

### 라운드 7 — 언어별 예시 이름 · 로/으로 (2026-09-20, 26ffd0a)

실계정, 헤드리스 390×844, 마이크·API 키 없음. 계정 셋. 주 경로 로그인 → F11 **17탭**, 회귀 없음. `docs/TEAM.md` 10장대로 찾은 말을 같이 적는다.

**1) 예시 이름이 언어마다 — 닫힘.** 세 언어를 켜고 예시를 하나씩 넣었다.

| 언어 | F02 예시 행 | F03 상단 라벨 | F04 카드 라벨 | 홈 자료 행 | 홈 이유 | F11 돌아가기 |
| --- | --- | --- | --- | --- | --- | --- |
| 일본어 | 아침 기사 한 문장 | 아침 기사 | 아침 기사에서 | 아침 기사 | 다음은 協. 아침 기사에서. | 아침 기사로 돌아가기 |
| 영어 | 회의 노트 한 문장 | 회의 노트 (안내 1장) | — | 회의 노트 · 영어 · 뽑기는 다음 단계 | — | — |
| 스페인어 | 안내문 한 문장 | 안내문 (안내 1장) | — | 안내문 · 스페인어 · 뽑기는 다음 단계 | — | — |

세 자료가 다 있는 홈에 "아침 기사"는 두 번(일본어 행·이유 한 줄)뿐이고 영어·스페인어 자료는 어디서도 "아침 기사"로 불리지 않는다. `inputName`·`inputFrom`·`inputTo`·`exampleLabel` 을 세 언어로 따로 돌린 값도 같다.

**2) 로/으로 — 닫힘.** 일본어 자료의 첫 줄을 한글 제목으로 넣어 카드를 끝내고 F11: "아침 기사로"(받침 없음), "회의실로"(ㄹ), "안건으로"(ㄴ), "메모로". F04 는 "회의실에서"·"안건에서"·"메모에서", 홈 이유는 "다음은 力. 회의실에서.". `ro` 단독: 서울로·회의록으로·물로·집으로. 일본어 제목(会議メモ)은 소리를 모르니 "로"로 떨어진다(설계대로).

**3) 조사를 안 거친 자리 — 이 말로 찾아서 없었다.** `src` 전체(`.ts`·`.tsx`)에서 (a) `${…}` 또는 `}` 바로 뒤에 은·는·이·가·을·를·과·와·로·으로·도·의 가 오는 줄, (b) `aria-label`·`title`·`alt`·`placeholder` 속성값에 `${` 나 한글이 든 줄을 훑었다. (a) 는 F03 앵커 "협력의 협"의 "의" 하나뿐이고 이건 갈리지 않는 조사다. (b) 는 고정 문구뿐(예: "원어민과 내 억양 곡선", "마이크 끄기"). `ko.ts` 밖에서 조사를 고르는 코드가 없다.

**4) F13 → F17 — 미구현, 결함 아님.** F13 주 버튼은 아직 "영어로 어떻게 말하지"이고 누르면 `/talk/[id]`(F14)로 바로 간다. `/talk/[id]/guess` 는 404. FLOW 1′장의 F17 행과 F13 "내가 먼저 해 볼게"는 4단계 일감으로 남는다. 5장 항목은 새 FLOW 대로 바꿔 두었다.

**참고 화면** `design/screens/*.html` 전체를 "아침 기사·회의 노트·안내문·日経·수업 슬라이드·릴스"로 찾았다: 아침 기사 10(그중 "아침 기사 한 문장" 1, "아침 기사에서" 1)·릴스 4·수업 슬라이드 2, 日経 0. 회의 노트·안내문은 참고에 없다 — 영어·스페인어 예시 화면이 참고에 없어서다(TEAM 10장 셋째: 코드로만 잡히는 것).

결함 없음. 막힘 0 · 헷갈림 0 · 다듬기 0.

### 라운드 8 — F17 추측 화면 첫 검증 (2026-09-20, ad4ce1b)

실계정, 헤드리스 390×844, 마이크·API 키 없음(영어 문장은 사전 조합이라 쓴 한국어 줄이 그대로 온다 — 키 없는 환경의 정상). 주 경로 로그인 → F11 **17탭**, 회귀 없음. `submitMissed` 가 바뀌었지만 일본어 축은 그대로다.

**1) 답이 새는 자리 — 없음.** F13 에 "이건 다음 스프린트로 미루죠"를 쓰고 "내가 먼저 해 볼게" → `/talk/[id]/guess`. 그 시점의 DB 행은 `text=''`, `attitude=null`, `meta={}`. HTML(18,390자)과 RSC 페이로드(`RSC: 1` 로 다시 받음, 11,495자)에 `"text":`·`english`·`"chunk":` 키가 없고 영문 단어 3연속이 0건. F17 에 머무는 동안 나간 문서 요청은 `POST /talk`(서버 액션)와 `/talk/[id]/guess`(+ `_rsc`) 뿐, `/talk/[id]`(F14) 프리페치 없음. 플레이스홀더 "영어로 한 줄", 입력 칸 `lang=en`. 화면 본문: "내가 하려던 말 / 이건 다음 스프린트로 미루죠 / 영어로 뭐라고 할 것 같아? / 틀려도 돼. 떠오르는 대로. / 이제 확인".

**2) 건너뛰는 길 — 없음.** 추측 전에 `/talk/[id]` 를 주소로 직접 열기 → `/guess` 로 돌아옴(DB `text` 는 여전히 빈 값). 새로고침 → `/guess`. 뒤로 → `/guess`. 같은 계정의 두 번째 탭에서 `/talk/[id]` → `/guess`.

**3) 채점 흔적 — 없음.** 추측에 "바나나 우주선이 날아간다 zzz"를 쓰고 "이제 확인" → F14. "내가 쓴 것"(`p`)과 "이 말"(`h1`) 두 줄의 DOM 값: 둘 다 20px · 700 · rgb(42,45,51) · 밑줄·취소선 없음 · 배경 없음. 본문에 정답·맞·틀·점수·%·일치도 0건, 취소선·붉은 글자 0개. 화면은 아무 판정을 하지 않는다.

**4) F14a — 통과.** `ELEVENLABS_VOICE_ID_EN` 이 없어 이쪽이 뜬다. 범례 "나"(녹음 0), 녹음 3회(API) 뒤 "나, 3회차"뿐이고 "원어민"이 없다. 한 줄 "아직 견줄 원어민 소리가 없어. 지금은 내 소리끼리 겹쳐 봐." 대체 텍스트 "내 억양 곡선 두 회차". 본문에 %·점·일치도 없음.

**5) 저장 — 통과.** 추측 뒤 DB: `meta.guess = "바나나 우주선이 날아간다 zzz"`, `text` 는 그때 채워짐(`content_source: fallback`). `chunks` 열은 id·user_id·lang·situation·text·attitude·intensity·node_id·input_id·nuance·created_at·meta — 판정·점수 칸이 없다.

**6) 빈 칸·이탈 — 통과.** 빈 칸이면 "이제 확인"이 꺼져 있고, 썼다 지우면 다시 꺼진다. F17 상단 라벨 → F13, F13 상단 라벨 → 홈(두 탭). F14 상단 라벨 → F13. 홈 "못 한 말" 행은 추측 전에도 뒤에도 "못 한 말 1개" → F13.

결함 없음. 막힘 0 · 헷갈림 0 · 다듬기 0. FLOW 에 없어 PM 에 묻는 것 둘(등급 없음):
- F17 에서 쓰다가 보내기 전에 상단 라벨로 나갔다 돌아오면 입력 칸이 비어 있다(DB 에도 없다). "쓴 말은 그대로 저장"은 보낸 뒤의 규칙이라 결함으로 안 올렸다. 보내기 전 이탈을 어떻게 볼지.
- F14a 에서 화면을 다시 열면 회차 수만 이어지고 직전 회차의 곡선은 안 불러온다. 이번 세션 첫 녹음은 겹칠 상대가 없다(범례도 "나, n회차" 하나). "내 소리끼리 겹쳐 봐"가 세션을 넘어서도 성립해야 하는지. 추측 뒤에 `/guess` 를 다시 열면 빈 추측 화면이 또 나오는 것도 같이(다시 보내면 추측이 덮인다).

### 라운드 9 — 추측 한 번 · 곡선이 세션을 넘는가 · 쓰다 만 글 (2026-09-20, db830a0)

실계정, 390×844. 이번엔 Chromium 을 가짜 마이크로 띄워(`--use-fake-device-for-media-stream`, 권한 `microphone`) **실제로 녹음했다.** API 키는 없다.

**주 경로 — 18탭, 회귀 없음.** 17 이 아닌 이유: 가짜 마이크가 있으니 O03 가 "듣는 중"이 되고 인식 0개로 "다 읽었어"를 눌러 O03b(마이크 문구) → "첫 카드로"가 한 탭 더 든다(읽기 시작 · 다 읽었어 · 첫 카드로). 인식이 4개 이상이면 F04 로 바로 가서 17 이다. 마이크 없이 돌면 읽기 시작 → 자동 O03b → 첫 카드로로 역시 17. 인식 0개는 `kana.status: recheck, supported: true, recognized: 0` — 통과로 남지 않는다(10장 (3) 닫힘).

**1) 추측은 한 번 — 통과.** 못 한 말: 추측 "banana spaceship" 뒤 `/talk/[id]/guess` 를 다시 열기·새로고침·뒤로·두 번째 탭 → 전부 F14. DB 의 `meta.guess`·`text`·`meta.chunk` 전과 후 같음. F17 HTML·RSC 페이로드에 `done` 문자열 0(서버 안에서만 쓰인다), `"text":`·`guess` 키 0, 영문 3연속 0. 한자 카드: 정답을 본 뒤 `/cards/[id]/3` → `/4`, DB `guess`·`guess_correct` 그대로.

**2) 곡선이 세션을 넘는가 — F14a 통과.**

| 한 것 | 범례 | 곡선(path d 길이) |
| --- | --- | --- |
| 녹음 0 | 나 | 0, 0 |
| 가짜 마이크로 1회 녹음 | 나, 1회차 | 0, 161 |
| 홈으로 나갔다 다시 열기 | 나, 1회차 | 0, 161 (그려진다) |
| 다시 녹음(2회) | 나, 1회차 / 나, 2회차 | 161, 161 |
| API 로 3건 더 넣고(5회) 다시 열기 | 나, 5회차 | 0, 33 |
| 다시 녹음(6회) | 나, 5회차 / 나, 6회차 | 33, 161 |

겹치는 것은 직전 하나뿐(곡선 둘), 첫 회차는 범례 하나, 숫자는 실제 녹음 횟수, 수치(%·일치도) 없음. 대체 텍스트 "앞 회차와 이번 내 억양 곡선".

**3) 쓰다 만 글 — 통과.** 하이드레이션 뒤에 읽었다(그 전엔 빈 값으로 보인다 — 서버 HTML 은 초안을 모른다).

| 화면 | 쓰고 나갔다 돌아오면(클라이언트 이동 / 새로 열기) | 다른 칸 | 보낸 뒤 다시 열면 |
| --- | --- | --- | --- |
| F02 일본어 | "초안 일본어 協力" / 같음 | 영어 칸 "" | "" · localStorage 없음 |
| F13 | "쓰다 만 못 한 말" / 같음 | — | "" |
| F17 (A 줄) | "my draft guess" | 새 줄 B 의 칸 "" | localStorage 없음, DB `meta.guess` 에 저장 |
| Scene3 | "쓰다 만 추측" | — | localStorage 없음, DB `guess` 저장, `/3` → `/4` |

localStorage 키는 `anchor:draft:input:ja` · `missed` · `guess:<id>` · `card:<id>` 로 갈린다. "임시·저장됨·초안" 류 문구 0. **계정 삭제 확인 칸은 반대로** 나갔다 오면(클라이언트 이동·새로 열기 둘 다) 비어 있고 "지울게"가 없다. localStorage 를 막은 브라우저(접근이 throw)에서 F13·F02·삭제 칸에 글자를 칠 수 있고 주 버튼이 켜지며 pageerror 0.

| 등급 | 항목 | 누른 것 → 보인 것 | 근거 |
| --- | --- | --- | --- |
| 헷갈림 | F10 을 다시 열면 범례는 "나, N회차"인데 내 곡선이 없다 | 協 카드 F10 에서 가짜 마이크로 1회 녹음(곡선 그려짐, DB 피치 148점) → F11 → API 로 2건 더 → `/cards/[id]/speak` 다시 열기: 범례 "원어민 / 나, 3회차", 곡선 d 길이 0, 0. `speak/page.tsx` 가 `countRecordings` 만 읽고 `lastPitch` 를 안 읽는다(F14 는 읽는다) | 1′장 F14 행의 규칙과 같은 모양. PM 이 9차 기준으로 "내 곡선은 다시 열어도 그려져야 한다" |

FLOW 에 없어 PM 에 묻는 것(등급 없음): F10 은 `hasNativeVoice` 를 안 본다. 원어민 음성 키가 없는 이 환경에서도 범례가 "원어민 / 나"이고 원어민 곡선은 비어 있다(브라우저 음성은 곡선이 안 나온다). F14 는 같은 조건에서 F14a 로 바뀐다. F10 에도 "원어민이 없으면 앞 회차" 규칙을 적용할지.

### 라운드 10 — 재만남 F12 첫 검증 (2026-09-20, 5fae641)

실계정, 390×844, 가짜 마이크. API 키가 없어 F12 의 읽기는 안 만들어지므로 `inputs.meta.readings` 에 **덩어리마다 서로 다른 가짜 읽기**(よみああ·よみいあ…)를 직접 넣고, 어느 읽기가 어디에 나오는지를 문자열로 찾았다.

**자료 A(예시 기사, 한자 21·덩어리 16).** 開 알아, 次 몰라, 協 카드는 "엉뚱한 답"(판정 "다름", `guess_correct=false`)으로 끝냄, 支 카드는 빈 추측으로 끝냄. 만난 것 = 開·協·支.

| 본 것 | 결과 |
| --- | --- |
| 읽기가 새는가 | 가려야 할 덩어리 15개의 읽기가 HTML·RSC 페이로드에 **0건**. 보여야 할 支 의 읽기는 화면·HTML·RSC 에 있다 |
| 덩어리 단위 | 協力(協 만남·力 아님)·開発(開 만남·発 아님) → 읽기 빈칸. 支 → 읽기 보임 |
| 틀린 추측으로 끝낸 카드 | 協 이 만난 것으로 셈(글자색 accent, 개수에서 빠짐) |
| 몰라 | 次 진하고 빈칸 |
| 개수 한 줄 | "새로 배울 건 18개."(21 − 3). 이름을 늘어놓지 않는다 |
| 나가는 길 | 상단 라벨 "아침 기사" → F03, F03 → 홈(두 탭). 주 버튼 "읽기 끝" → 홈. 주 버튼 1개 |
| 글자색 | T1·T2·accent(#3F5E90)뿐. 채도 높은 색·그린·앰버 없음 |

**자료 B(協力して妥協した。).** 協·力 알아 → 妥協 은 協 만 만남·妥 는 아님: 읽기 빈칸, "새로 배울 건 妥 하나뿐.", 妥協 의 읽기는 페이로드에 없음. 妥 도 알아로 하면 두 덩어리 읽기 다 보이고 개수 줄이 **사라진다**(아래 질문). **자료 C(읽기 없음).** 만난 글자(安·支)에 빈칸이 안 달리고 그대로, 안 만난 덩어리(運転)만 빈칸.

**켜진 경로 경계(PM 추가 요청, FLOW 3397f9d 뒤).** 사용자가 손댄 적 없는 경로로 켜진 상태는 화면으로는 못 만들어서(씨앗·추론을 켜는 흐름이 아직 없다) `user_node_state` 에 직접 넣었다: 예시 기사의 日→`inferred`, 車→`onboarding`, 新→`default`, 作→`talk`(넷 다 `knows_meaning=true`). 사용자가 켠 것은 F03 알아 開·力·安, 카드 協·支.

| 본 것 | 결과 |
| --- | --- |
| F12 틴트 | 만남 표시 글자 協·力·開·安·支 뿐. 日·車·新·作 은 진하고 그 덩어리(日本·車·新しい·作る)는 읽기 빈칸 — **통과** |
| F12 개수 줄 | "새로 배울 건 16개."(21 − 5). 넷을 새 것으로 셈 — 통과 |
| 홈 이유 한 줄 | "다음은 全. 安全な運転を支える。에서." 넷을 주어로 부르지 않음 — 통과 |
| **F03 「아는 것」 묶음** | 같은 자료의 F03 을 열면 「아는 것」 묶음이 **協力車開日作新安支** — 손댄 적 없는 日·車·新·作 이 알아로 고른 것과 나란히 선다. 아래 표 |

**회귀.** 영어 자료 `/read` → 그 자료의 뽑기(`/inputs/[id]`). 없는 id → 404 "이 페이지는 없어.". F10(9차 건): 가짜 마이크 1회 + API 2건 뒤 다시 열면 범례 "나, 3회차"에 **곡선이 그려지고**, "원어민"이 아니라 앞 회차 방식(대체 텍스트 "앞 회차와 이번 내 억양 곡선", "아직 견줄 원어민 소리가 없어…"), 다시 녹음하면 "나, 3회차 / 나, 4회차" 둘 — 닫힘.

| 등급 | 항목 | 누른 것 → 보인 것 | 근거 |
| --- | --- | --- | --- |
| 다듬기 | 만난 한자에 배경 틴트가 안 칠해진다 | F12 A 에서 만난 글자(協·開·支)의 계산된 배경이 `rgba(0,0,0,0)`, 글자색만 #3F5E90. `.metSeen` 이 `var(--color-accent-bg)` 를 쓰는데 그 변수가 어디에도 정의돼 있지 않다(다른 틴트는 `--color-tint`). 만난 것과 아닌 것이 글자색으로만 갈린다 | 1′장 F12 "틴트" · CLAUDE.md "강조는 글자색이 아니라 배경 틴트" |
| 다듬기 | F12 에 `h1` 이 없다 | 라벨 "아침 기사"와 본문 카드뿐, 제목 태그 0 | CLAUDE.md 화면마다 h1 하나 |
| 헷갈림 | F03 이 손댄 적 없는 한자를 「아는 것」이라 부른다 (PM 추가 요청 뒤 발견) | `source` 가 inferred·onboarding·default·talk 인 日·車·新·作 을 넣고 예시 기사의 F03 을 열면 「아는 것」 묶음에 알아로 고른 開·力·安 과 같이 선다. F12 는 같은 넷을 새 것으로(빈칸·개수 16), 홈 이유 줄도 부르지 않는데 F03 만 아는 것으로 부른다. F03 이 `knows_meaning` 만 보고 `source` 를 안 본다 | FLOW 4장·8행 "「아는 것」 칸에도 판정된 노드만", 3397f9d "추론으로 올라간 노드는 이름을 부르지 않는다". 코드 주석(`src/lib/db/kanji.ts` 42행)도 같은 말 |

구현과 참고가 같이 FLOW 와 다른 것 — PM: 오른쪽 아래 알약 "재만남". 알약 어휘는 켜짐/꺼짐/이어서 셋뿐인데(4장) 참고 F12.html 도 "재만남" 알약을 둔다.

FLOW 에 없어 PM 에 묻는 것(등급 없음):
- **새로 배울 게 하나도 없을 때** 개수 줄이 통째로 없다(B 전부 만남: 본문과 "읽기 끝"만). 무슨 말을 할지.
- ~~**다른 자료에서 만난 한자도 틴트**다. 자료 C 의 支 는 C 에서 판정한 적이 없는데 A 의 카드로 끝냈으니 틴트다(노드 상태는 계정 전체). "이 자료에서 만났는가"를 자료 단위로 볼지 계정 단위로 볼지.~~ → **통과.** PM 이 준 기준 문장("이 자료에서")이 좁았다. FLOW 3397f9d: 어느 자료에서 켜졌는지는 보지 않고, 가르는 것은 **사용자가 행동으로 켰는가**(`source` 가 `input`·`card`).
- 참고 F12.html 의 "妥協, 협은 방금 봤지 / 타협." 줄을 구현이 뺐다(개발: 한자어의 한국어 읽기를 믿을 값이 없어 기획에 물음). 어느 쪽이 기준인지.

### 라운드 11 — MVP 5단계 닫힘: F15 첫 검증 · F12 재고침 · design:diff 첫 실행 (2026-09-20, 57a0f37)

실계정, 390×844, 가짜 마이크. 로그인 → 예시 기사 → F03 → 가나(듣는 중 0/6 → "다 읽었어") → 카드 5장면 → F10(듣기·말하기 녹음 1건) → F11 **18탭**(가짜 마이크 경로, 전과 같다). 영어는 설정의 언어 추가로 켜고 F13 → F17 → F14(녹음 1건) → 회색 "됐어" → **F15**. 10차 다듬기 둘: F12 만난 글자의 계산된 배경 `rgb(233,238,246)`, `h1` "아침 기사" 하나 — **둘 다 닫힘**.

**F15 하루 끝.** 세 상태로 열었다.

| 상태 | 보인 것 |
| --- | --- |
| 카드 1(協) 끝, 영어 녹음 1, 나머지 20자 남음 | "協 / 한자 1 / 켜짐" · "이건 다음 스프린트로 미루죠 / 말해본 덩어리 1 / 켜짐" · "**또 만날 글자 20** / 내일 기사에 또 나오면"(알약 없음). F12 는 "새로 배울 건 20개." — 같다 |
| 支 빼고 전부 알아 | 셋째 행 "支 / 내일 기사에 또 나오면". F12 "새로 배울 건 支 하나뿐." — 같다. 홈은 아직 홈(이유 "協을 맞혔으니 支가 가장 가까워.") |
| 支 카드도 끝 → 큐 빔 | 홈을 열면 `/today/done` 으로 온다. "協 支 / 한자 2 / 켜짐", 말해본 덩어리 1, 셋째 행 없음. F12 "새로 배울 건 없어. 다 만난 글자야." |

칭찬·점수·%·연속·스트리크 문구 0. "재만남"·"대기" 0. 알약은 앞 두 행의 "켜짐" 둘뿐. `h1` "오늘 켜진 것" 하나. 상단 라벨 "오늘 끝" → 홈, "내일 아침에" → 홈(큐가 남아 있을 때). 한자 n 은 오늘 끝낸 카드 수(DB `landed_at` 오늘)와 같다. **분·시간은 F15 를 포함해 어느 화면에도 없다** — 열한 화면의 본문을 `\d+분|\d+시간` 으로 찾았다. 상단 오른쪽의 "오전 6:30" 은 시각이지 공부 시간이 아니고 참고 화면에도 같은 자리에 있다.

**F12 재고침.** 알약 0. 0개일 때 "새로 배울 건 없어. 다 만난 글자야." 가 뜬다(블록 안 사라짐). 한국어 낱말: `協力して妥協した。` 에 協(카드)·力(알아)만 만난 상태 → "妥協, 협은 방금 봤지", 妥協 읽기 빈칸, **"타협"은 화면·HTML·RSC 어디에도 없다**. 妥 까지 알아 → 妥協 읽기가 보이고 "새로 배울 건 없어…" — 그런데 "타협"도 짚는 말도 없다(아래 표). 손댄 적 없는 경로 경계는 10차 보강과 같다.

**design:diff 첫 실행.** 미리보기 서버로 22장 **52초(첫 컴파일 포함)·45초(두 번째)**, 두 번의 수치가 같다. 실계정 서버에 돌리면 O01 만 대조되고 21장이 "열리지 않음 (200 → /?next=…)"으로 나온다 — 0% 로 안 세고 따로 묶어 준다. 목록은 읽힌다. 위에서 일곱을 열었다(참고·구현·차이를 나란히 붙여서 봤다 — 도구는 세 파일을 따로 남긴다):

| 순위 | 화면 | 연 결과 |
| --- | --- | --- |
| 1 | F16 9.25% | SCREENS.md 가 이미 적어 둔 `h1` 건. 새것 없음 |
| 2 | F12 4.53% | 본문 줄바꿈이 달라 전부 빨감(정상인 차이 1). 그 안에 진짜 하나 — **참고는 `協力` 을 한 덩어리로 틴트하는데 구현은 글자마다 틴트라 낱말 안에 틈이 보인다**("協 力"). 아래 다듬기 |
| 3 | F14 4.18% | 곡선·회차·아래 한 줄이 예시 데이터 차이. 자리·크기 같음 |
| 4 | O01 3.54% | 구현에 로고 마크가 있고 참고에 없다. CLAUDE.md 로고 확정 뒤 참고가 낡은 것 |
| 5 | Scene5 3.27% | **참고는 착지 낱말 하나(協力), 구현은 둘(協力·妥協)**. 참고가 57a0f37 에서 FLOW 91행대로 고쳐졌고 구현이 안 따라왔다. 아래 헷갈림 |
| 6 | F01 3.13% | 자료 이름·개수가 예시 차이. 자리 같음 |
| 7 | F03 3.07% | **행의 한자가 참고 30px, 구현 18px**(`[lang=ja]` 계산값). 아래 다듬기 |

도구 자체: 돌아가고, 매 라운드 돌릴 만하다(1분). 읽는 법은 SCREENS.md 세 가지 "정상인 차이"로 충분했다 — F12·F14·F01 이 그대로 그 셋이었다. 아쉬운 것 둘. (1) 참고·구현·차이가 세 파일이라 열어 볼 때 나란히 붙여야 한다 — 한 장(3분할)으로 남기면 여는 시간이 준다. (2) `SKIP` 의 "E01~E06 영어 축 — 아직 구현 없음"은 영어 축(F13·F17·F14)이 있는 지금 무슨 뜻인지 모르겠다 — E 시리즈가 어느 화면인지 이유를 다시 적어야 빠뜨린 것과 못 부르는 것이 갈린다. 판정을 안 하는 건 맞다 — 위 일곱 중 참고가 낡은 게 둘(O01·F16), 구현이 늦은 게 둘(Scene5·F03), 정상이 셋이었다.

| 등급 | 항목 | 누른 것 → 보인 것 | 근거 |
| --- | --- | --- | --- |
| **막힘** | 카드를 다 끝내면 홈에 못 돌아간다 | 큐가 비면 홈(`/today`)이 `/today/done` 으로 보낸다. F15 의 탭은 "오늘 끝"과 "내일 아침에" 둘뿐이고 둘 다 `/today` 라 **다시 F15 로 온다**. 자료 넣기·못 한 말·설정·재만남(F03 경유)으로 가는 길이 없다. 주소를 직접 치면 `/inputs/new`·`/settings` 는 열린다. 첫 방문에 예시 기사의 한자를 전부 알아로 찍기만 해도 이 상태다 | CLAUDE.md "뒤로 갈 길이 없는 화면", "어느 화면에서도 홈까지 2탭". FLOW 1′장 56행(큐 비면 F15)과 F15 행(내일 아침에 → 홈)이 서로를 가리킨다. 적는 사이 **c716f7b 가 FLOW 를 고쳤다**(F15 는 마지막 카드 착지 직후에만, 홈은 보내지 않는다) — 57a0f37 에서는 구현이 FLOW 대로였고, 지금은 홈의 `redirect("/today/done")` 이 FLOW 와 다른 것이라 개발 몫이 됐다 |
| 헷갈림 | Scene5 착지 낱말이 안 만난 한자의 읽기·한국어를 준다 | 協 카드 Scene5: "協力 きょうりょく 협력 / **妥協 だきょう 타협**". 妥 는 같은 기사의 다음 카드고 아직 안 만났다. 支 카드: "支援 しえん 지원 / 支店 してん 지점". 같은 妥協 을 F12 는 가리는데 Scene5 는 연다. 씨앗 `kanji-cards.json` 의 `landing` 을 만난 글자로 거르지 않는다 | FLOW 4장 91행 "착지의 낱말은 그 카드의 한자와 이미 만난 글자로만"(554be2b). 원칙 1. 참고 Scene5.html 은 57a0f37 에서 한 낱말로 고침 |
| 헷갈림 | 妥 까지 만난 뒤에도 "타협"이 안 보인다 | `協力して妥協した。` 에 協·力·妥 다 만남 → 妥協 읽기는 보이고, 아래 줄은 "새로 배울 건 없어…"만. "타협"은 HTML·RSC 에 없다. 코드 주석이 "짚은 낱말은 섞인 것이라 지금은 늘 가려진다"고 스스로 적어 둠 — 보이는 조건이 없는 값 | PM 이 준 기준 "둘 다 만나면 보여야, 읽기 가림과 어긋나면 결함". FLOW 는 "전부 만났을 때만 보인다"(필요조건)라 **보여야 한다고는 안 적었다** — 등급은 PM 기준으로 매겼고 FLOW 문장으로는 등급 없음 |
| 다듬기 | F12 틴트가 낱말 안에서 끊긴다 | 만난 글자마다 `padding: 0 5px` 틴트 span 이라 "協 力"처럼 낱말 사이에 흰 틈. 참고 F12.html 은 `協力` 한 span | 참고 화면 그대로(작업 방식) |
| 다듬기 | F03 행의 한자가 작다 | `[class*=row] [lang=ja]` 계산값 18px/700. 참고 F03.html 30px/700 | 참고 화면 그대로. CLAUDE.md "한자 표시 44~132px"에는 둘 다 못 미친다 |

FLOW 에 없거나 FLOW 와 구현이 다른 것(등급 없음, PM):
- **시간 한 줄이 없다.** FLOW 38행 "따로 공부한 시간 n분…"이 F15 에 없다. 개발 768acd4: 잴 값이 `recordings.duration_ms`(녹음 3초)뿐이라 지어내지 않고 뺐다. 참고 F15.html 에는 "11분"이 있다. FLOW 를 고칠지, 재는 법을 정할지.
- **"말해본 덩어리"에 F10 이 안 든다.** 協力 을 F10 에서 말했는데(녹음 `card_id`) 세지 않고, `chunk_id` 녹음(영어)만 센다. 덩어리를 영어 대화 덩어리로만 볼지.
- **아무것도 안 켠 날의 F15.** 예시 기사 21자를 전부 알아로 찍으면(카드 0·녹음 0) 홈이 F15 로 보내고, 카드에 "그래프" 라벨만 있고 행이 없다. 무슨 말을 할지 — 위 막힘과 같은 자리다.

### 라운드 12 — 11차 다섯 건 닫힘 확인 · 전체 내보내기 첫 검증 · design:diff 재실행 (2026-09-21, 219c854)

실계정, 390×844, 가짜 마이크. 주 경로 로그인 → F11 **18탭**(가짜 마이크 경로, 11차와 같다).

**11차 다섯 건 — 다 닫혔다.**

| 건 | 누른 것 → 보인 것 |
| --- | --- |
| 홈 고리 (c240e6f) | 아침 기사 21자 + `協力して妥協した。` 3자를 전부 끝낸 상태로 홈을 열면 `/today` 에 머문다. 자료 행 둘이 "한자 21개 · 다 봤어" → `/inputs/[id]/read`(F12), 자료 넣기 → F02, 설정 → F16, 영어를 켜면 못 한 말 → F13. 이유 줄 "이 자료는 다 봤어. 내일 또 나오면 그때 만나." 支 를 마지막으로 착지한 F11 의 주 버튼이 "오늘 켜진 것" → `/today/done`, 자료 하나(森の中で休む。)를 더 넣어 카드가 남게 하고 같은 F11 을 열면 "홈으로" → `/today`. F15 의 "내일 아침에" → 홈에 머문다. **첫 방문에 예시 기사 21자를 전부 알아로 찍는 경로**: F03 주 버튼이 "홈으로"가 되고, 홈은 튕기지 않고 자료 행 → F12 ("운전. 새로 배울 건 없어. 다 만난 글자야."), F12 상단 라벨 → F03(주 버튼 홈으로) → 홈 두 탭 |
| Scene5 착지 (0faefa8) | 妥 를 안 만난 상태의 協 카드 Scene5·F10: 妥協·だきょう·타협 이 화면·HTML·RSC 에 0건. 妥 를 알아로 찍은 뒤 같은 Scene5: "協力 きょうりょく 협력 / 妥協 だきょう 타협" 둘. 支 카드(씨앗 착지 支援·支店, 援·店 안 만남): 낱말 0 → "支 し 지출" 하나, 支援·しえん 페이로드 0건 (아래 질문) |
| "타협" (df20a9a) | 協·力 만남, 妥 아직 → "妥協, 협은 방금 봤지 / 새로 배울 건 妥 하나뿐." 타협 0건. 妥 도 만남 → "타협. 새로 배울 건 없어. 다 만난 글자야." 타협이 HTML·RSC 에 있다 — 양쪽이 갈린다 |
| 틴트 묶음 (ed925e3) | 아침 기사 다 만남: 상자가 協力·次世代·車·開発·両社·日·協力·車·基盤·作·発表·新·基盤·安全·運転·支 — 낱말째 하나씩. `協力して妥協した。` 에 妥 아직: 상자 協力·協 — 妥協 은 協 만 따로 갈린다(규칙대로). 배경 `rgb(233,238,246)` |
| F03 30px (219c854) | 행 한자 `[lang=ja]` 계산값 30px/700 |

**전체 내보내기 — 처음 봤다.** 설정 "전체 내보내기" 행을 탭하면 `anchor-export-2026-09-20.json` 이 내려오고 화면은 설정에 남는다. `GET /api/export` 200, `application/json`, `attachment`. 파싱된다. 내가 넣은 것: 자료 3(본문·읽기 meta.readings 2건), 카드 2(추측 "힘을 합친다"·정오·착지 시각), 녹음 1(피치 148점·3002ms, 원본 키 null), 노드 상태 22(source input 20·card 2) — 표마다 파일 행 수가 DB 의 내 것과 같다. 경계: 다른 계정(`qa-other`)의 자료 `他人の資料` 와 노드 상태를 DB 에 직접 넣고 다시 뽑으니 **파일에 없다**(users·inputs·user_node_state 남의 행 0). 공용 노드 3,301개는 파일 nodes 에 0. `account_deletions`(삭제 원장)·`node_cards`(공용 카드 캐시)는 안 들어간다 — 사용자 데이터가 아니라 맞다. 로그인 없이 `/api/export` 를 치면 307 → `/`, 브라우저로 열어도 로그인 화면.

**design:diff 재실행.** 22장 50초/46초, 두 번 같음. Scene5 3.27% → **2.44%(5위 → 12위)**, 열어 보니 참고와 같은 한 낱말. F03 3.07% → **3.11%** — 한자는 30px 로 맞았는데 행이 참고보다 8px 씩 높아(30px 글자의 줄 높이) 아래 카드가 40px 내려가 수치가 오히려 올랐다. F12 4.53% → 4.53% — 틴트 묶음은 고쳐졌는데(열어 보면 協力 한 상자) 본문 줄바꿈 차이가 화면 전체를 빨갛게 만들어 **그 변화가 숫자에 안 잡힌다**. 도구가 안 본 게 아니라 정상인 차이 1이 진짜 변화를 삼키는 자리다. 새로 올라온 것 없음(F16·F12·F14·O01·F01 순서 그대로).

| 등급 | 항목 | 누른 것 → 보인 것 | 근거 |
| --- | --- | --- | --- |
| 헷갈림 | 내보낸 노드 상태가 어느 한자인지 파일만으로는 알 수 없다 | `user_node_state` 22행이 `node_id`(uuid)·knows_meaning·source 만 들고 있고, 그 노드는 공용(user_id 없음)이라 파일 nodes 에 0건. 내가 알아로 찍은 20자가 무엇인지 파일에서 못 읽는다. 카드 2건은 payload 에 한자가 있어 읽힌다 | 라우트 주석 "서비스가 사라져도 학습 기록은 남는다", CLAUDE.md 전체 내보내기. 공용 참조를 빼는 경계 자체는 맞다 — 상태 행에 한자 키를 같이 적을지는 PM |

FLOW 에 없는 것(등급 없음, PM):
- **착지 낱말이 하나도 안 남을 때의 모양.** 협 카드(力 아직)와 支 카드에서 "協 きょう 협력", "支 し 지출" — 일본어는 한 글자·그 글자의 읽기, 한국어는 두 글자 낱말이라 한 줄에 다른 두 낱말이 선다. F10 도 같은 것을 받아 첫 카드에서 사용자가 "協 / きょう" 한 글자를 말한다. 주 경로 첫 카드가 지금 이 모양이다(力 이 아직이라 協力 이 걸러진다). FLOW 91행은 "앵커 단어(후킹에 쓴 한국어 낱말)"라고만 한다 — 일본어 쪽을 무엇으로 둘지.
- **F12 "운전."이 짚는 말 없이 선다.** 支 만 아직인 아침 기사: 섞인 낱말이 없어 마지막으로 다 만난 낱말(運転)의 한국어가 "운전. 새로 배울 건 支 하나뿐."으로 붙는다. 위에 "運転, …" 짚는 줄이 없어 왜 운전인지 화면이 말하지 않는다. 다 만난 뒤 "운전. 새로 배울 건 없어."는 df20a9a 가 의도한 모양.
- 홈 이유 줄 "**이 자료는** 다 봤어"가 자료 둘을 다 본 상태에서도 단수다.
- `encounters` 표를 쓰는 코드가 없다(`INSERT INTO encounters` 로 `src` 검색 0건). 재만남 인식률(검증 기준)을 잴 자리가 아직 비어 있다 — 6단계 c716f7b 몫으로 보인다.

## 10. 덮이지 않은 구멍 — 키가 생기면 이것부터 본다

결함이 아니라 **아직 아무도 보지 못한 자리**다. 후리가나에서 지금까지 검증된 것은 **"지어내지 않는다"까지다**(읽기 개수가 어긋나면 문장 통째로 ruby 없음, 개별 불량이면 그 덩어리만 없음, 금지 자리 ruby 0). 맞게 붙는지는 본 적이 없다. 이 절을 지우기 전까지 후리가나를 "검증 끝"으로 읽지 않는다.

| 무엇 | 왜 지금 못 보나 | 걸리는 원칙 |
| --- | --- | --- |
| (1) 읽기가 그 단어의 읽기와 맞는지 (協力 → きょうりょく) | `ANTHROPIC_API_KEY` 가 없어 카드 payload 에 `source.readings` 가 오지 않는다 | 틀린 읽기를 사실처럼 보여 주는 것 |
| (2) **가린 자리와 안 가린 자리가 섞이지 않는지** — 맞혀야 할 한자만 가려지고 나머지는 다 보이는지 | 같음. 읽기가 있어야 "가려진 것"과 "안 붙은 것"이 구분된다 | **원칙 1.** 대상 한자의 읽기가 새면 추측 전에 답을 주는 것이고, 반대로 다른 한자까지 가려지면 읽을 수 있는 것을 못 읽게 막는다 |

(2)가 원칙에 직접 걸리는 쪽이다. 키가 생기면 (2)부터 본다.

후리가나 밖에도 지금 환경으로는 못 보는 자리가 있다. 라운드 기록의 "미검증"을 여기 모은다. 키·마이크가 생기면 같이 본다.

| 무엇 | 왜 지금 못 보나 | 걸리는 것 |
| --- | --- | --- |
| ~~(3) O03 인식 0개 "다 읽었어"~~ | 라운드 9 에서 가짜 마이크로 봤다 — 통과가 아니라 recheck 로 남는다 | 닫힘 |
| ~~(4) 마이크로 실제 녹음·곡선~~ | 라운드 9 에서 가짜 마이크로 봤다 — 3초 녹음 → 피치 148점 저장 → 곡선. 원본 오디오 업로드(Blob 토큰)는 여전히 못 본다 | 곡선은 닫힘, 원본 저장만 남음 |
| (5) 원어민 곡선, "내 목소리 버전" 듣기 | `ELEVENLABS_API_KEY` 가 없어 `/api/tts` 가 204 이고 브라우저 음성으로 간다 | 원칙 3 |
| (6) `error` 화면의 "다시 시도"가 오류 난 화면을 다시 그리는지 | 미리보기·실계정 어느 쪽으로도 오류를 일부러 낼 길이 없다. 코드로만 봤다 | 1′장 error |
| (7) F15 "말해본 덩어리" 행에 영어 덩어리("push this to")가 서는지 | `ANTHROPIC_API_KEY` 가 없어 못 한 말에서 영어 문장·덩어리가 안 만들어지고, 행 제목에 한국어 못 한 말("이건 다음 스프린트로 미루죠")이 그대로 선다. 로컬 한계지 결함이 아니다 | 1′장 F15 |
| (8) 내보내기에 영어 덩어리(chunks)·녹음 원본 키(audio_object_key)·encounters 가 들어가는지 | 로컬엔 영어 덩어리가 안 만들어지고(API 키), Blob 토큰이 없어 원본 키가 null 이며, encounters 는 코드 어디서도 쓰지 않는다(`INSERT INTO encounters` 로 `src` 검색 0건). 표는 다 있고 행이 0 | 데이터 원칙 내보내기 |
| (9) **F03 "발판 없음"이 거짓인 행** — 재확인 대기 | PM 진실이 찾아 개발에 넘김(README M0). DB 로 확인: 공용 한자 2,136자 중 한국 한자음(`ko_sound`)이 있는 것 2,130, 앵커 낱말(`ko_word`)이 있는 것 654 — **소리는 있는데 낱말이 없는 1,477자**가 F03 에서 "발판 없음 / 아직 아는 소리가 없음"으로 선다(`anchor = ko_word && ko_sound`). 지금까지의 QA 흐름은 두 묶음이 갈리는지만 봤지 묶음의 말이 참인지는 안 봤다. 개발 수정 뒤: 소리만 있는 한자(예: 妥 가 아닌 것)를 자료에 넣어 어느 묶음에 서고 부제가 무슨 말을 하는지 본다 | 원칙 2 · README M0 "화면이 사실이 아닌 것을 말하는 자리" |
| (10) **Scene2 부품 칸의 빈 네모** — 재확인 대기 | PM 진실이 찾아 개발에 넘김. 씨앗 parts 에 BMP 밖 문자 53종, 든 한자 170자. design b285f10 이 그 부품을 빼고 셋(𧾷→足 등)을 갈아 끼웠다고 함 — 지금까지의 QA 흐름은 부품 칸의 글자가 실제로 그려지는지(두부) 안 봤다. 재확인: 路 같은 한자를 자료에 넣어 Scene2 부품 칸의 글자마다 `document.fonts.check` 와 실측 폭으로 두부인지 본다 | 원칙 1 (추측할 재료) · README M0 |
