/**
 * 앵커 낱말의 **모양** 규칙 둘, 그리고 지금 어긋나 있는 줄의 선언.
 *
 * 고르는 규칙(`ko-sound.ts`)과 다른 일이라 파일을 나눈다 — 저기는 "여러 소리 중 어느 것"이고
 * 여기는 "그 낱말이 앵커로 설 수 있는 모양인가"다.
 *
 * **규칙 1 — 앵커는 그 글자의 소리와 같을 수 없다.** `judge-items.ts` 가 F03 부제를
 * `${ko_word}의 ${ko_sound}` 로 짜므로 둘이 같으면 화면에 **"역의 역"·"몽의 몽"** 이 선다.
 * 게다가 `hasWord` 가 참이 되어 그 글자가 **첫 묶음 "아는 낱말에서 시작"** 에 서는데,
 * 가진 것은 소리뿐이다. 둘째 묶음 "부를 낱말이 아직 없어"가 정확히 이 경우인데 거기 안 간다.
 * **앱이 없는 것을 있다고 말하는 자리라, 흠이 아니라 거짓말이다.**
 *
 * **규칙 2 — 앵커는 두 글자다.** 착지(`landing`)가 "한국어와 일본어가 같은 두 글자 조합"만
 * 쓰기 때문이다(`card-content.ts` 의 landing 규칙). 세 글자 앵커는 착지가 못 선다.
 *
 * **규칙 3 — 앵커는 그 글자의 한국 한자음을 「글자 그대로」 품는다.** 두음법칙(료 → **요**리)도
 * 사이시옷(수 → **숫**자)도 **바뀐 소리**라 통과시키지 않는다(기획 `dfa1ea1`·`801c97f`).
 * 까닭은 카드다: `料` 를 「요리」로 부르면 발판은 "요" 인데 Scene1 은 `node.meta.ko_sound` 를
 * 그대로 찍어 **"이 료, 한자로는"** 이라 하고, 패턴 줄은 **"료 りょう"** 를 가르친다.
 * **앵커가 부른 소리와 카드가 가르치는 소리가 갈리면 발판이 발판이 아니다** — 그리고 료 → りょう 는
 * 맞지만 요 → りょう 는 틀리다. 한국 한자음에서 음독으로 가는 다리가 거기서 끊긴다(원칙 2).
 *
 * **`ko-sound.ts` 의 `headSound` 는 그대로 둔다.** 거기서 접는 것은 "이 앵커가 **어느 소리**를
 * 가리키나" 를 푸는 일이고(両/양사 → 량), 여기서 묻는 것은 "그러니 **앵커로 서도 되나**" 다.
 * 두 물음은 다르다 — 접어서 찾은 소리가 맞더라도, 낱말에 그 글자가 안 보이면 발판이 안 선다.
 *
 * 셋을 한 규칙으로 묶지 않는다. 1번은 **화면이 이미 새는 것**이고 2번은 **착지가 못 서는 것**이라
 * 고치는 사람도 급한 정도도 다르다. 묶으면 나중에 2번을 느슨하게 할 때 1번까지 같이 풀린다.
 *
 * 아래 두 목록은 **지금 어긋나 있는 줄**이다. 값은 그때 적혀 있던 낱말이라, 기획이 고치면
 * 시험이 "이 줄은 이제 안 어긋난다, 지워라"로 빨개진다. **목록이 비면 목록째 지운다.**
 * 어느 낱말로 바꿀지는 기획이 정한다(`docs/FLOW.md` 96: 낱말을 알면 표를 채우는 것이 먼저다).
 */

/** 규칙 1 을 어기는 줄 — 낱말이 그 글자의 소리와 같다. */
export const SOUND_ECHO_EXCEPTIONS: Record<string, string> = {
  "駅": "역",
  "湯": "탕",
  "猫": "묘",
  "玉": "옥",
  "町": "정",
  "県": "현",
  "州": "주",
  "泣": "읍",
  "妻": "처",
  "歳": "세",
  "夢": "몽",
};

/** 규칙 2 를 어기는 줄 — 낱말이 두 글자가 아니다. 규칙 1 의 열한 줄이 여기에도 든다. */
export const TWO_SYLLABLE_EXCEPTIONS: Record<string, string> = {
  "次": "차세대",
  "駅": "역",
  "遠": "원거리",
  "湯": "탕",
  "猫": "묘",
  "玉": "옥",
  "館": "도서관",
  "書": "도서관",
  "町": "정",
  "県": "현",
  "州": "주",
  "鉄": "지하철",
  "券": "승차권",
  "泣": "읍",
  "妻": "처",
  "歳": "세",
  "夢": "몽",
};

/**
 * 규칙 3 을 어기는 줄 — 낱말이 그 글자의 소리를 **바뀐 꼴로만** 품는다. 열다섯 다 두음법칙이다.
 *
 * 기획이 다섯은 바꿀 낱말을 댔고(`dfa1ea1`: 料→자료, 両→차량, 理→관리, 路→도로, 労→근로),
 * 나머지는 **바꿀 낱말이 없는 자리**라 "소리가 갈리는 자리로 안다" 고 적었다. 값은 기획이 정한다.
 */
export const LITERAL_SOUND_EXCEPTIONS: Record<string, string> = {
  "両": "양사", // 소리 량
  "旅": "여행", // 소리 려
  "練": "연습", // 소리 련
  "来": "내년", // 소리 래
  "例": "예외", // 소리 례
  "連": "연속", // 소리 련
  "冷": "냉장", // 소리 랭
  "緑": "녹색", // 소리 록
  "路": "노선", // 소리 로
  "料": "요리", // 소리 료
  "礼": "예의", // 소리 례
  "理": "이해", // 소리 리
  "老": "노인", // 소리 로
  "年": "연령", // 소리 년
  "労": "노동", // 소리 로
};
