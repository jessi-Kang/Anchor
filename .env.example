# ── Neon Postgres ─────────────────────────────────────────────────────────────
# 앱 런타임이 쓰는 연결. 반드시 RLS를 우회하지 못하는 `anchor_app` 역할이어야 한다.
# (Neon 콘솔에서 만든 역할은 neon_superuser 멤버라 BYPASSRLS가 있다. 마이그레이션이 SQL로 만든다.)
# 이름이 DATABASE_URL 이 아닌 이유: Vercel 의 Neon 통합이 소유자 연결을 DATABASE_URL 로 주입하기 때문.
ANCHOR_DATABASE_URL=postgresql://anchor_app:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# 마이그레이션·백업 전용 연결 (테이블 소유자 역할, 예: neondb_owner). 앱 코드에서는 절대 쓰지 않는다.
# 비어 있으면 스크립트가 DATABASE_URL (Vercel Neon 통합 주입값) 을 대신 쓴다.
DATABASE_URL_ADMIN=postgresql://neondb_owner:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# 0001 마이그레이션이 anchor_app 역할을 만들 때 쓰는 비밀번호 (ANCHOR_DATABASE_URL 의 비밀번호와 같아야 한다)
ANCHOR_APP_PASSWORD=

# ── Neon Auth (Google OAuth) ──────────────────────────────────────────────────
# Neon 콘솔 → 프로젝트 → 브랜치 → Auth → Configuration 의 Auth URL
NEON_AUTH_BASE_URL=https://ep-xxx.neonauth.region.aws.neon.tech/neondb/auth
# openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET=

# ── 백업 (scripts/backup, .github/workflows/backup.yml) ───────────────────────
# 배포 전 스냅샷: Neon API 키 + 프로젝트/브랜치 ID. 없으면 스냅샷 단계를 건너뛴다(프리뷰 배포용).
NEON_API_KEY=
NEON_PROJECT_ID=
NEON_BRANCH_ID=
# 매일 백업 (/api/cron/backup → Vercel Blob 비공개 스토어). Vercel 이 BLOB_READ_WRITE_TOKEN 을 주입한다.
CRON_SECRET=
BACKUP_RETENTION_DAYS=35
# (선택) pg_dump 를 S3 호환 스토리지에도 올리려면 .github/workflows/backup.yml 참고

# ── 외부 API (이후 단계) ──────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID_JESSI=pwjMkbtUbj1hBa0RkN5N
